# Operators

Where Clause
Using Operators in WHERE

Arithmetic Operators
: +(addition), -(subtraction), *(multiplication), /(division), %(modulus)
Comparison Operators:
= (equal to), != (not equal to), >, >=, <=
Logical Operators:
AND, OR, NOT, IN, BETWEEN, ALL, LIKE, ANY
Bitwise Operators:
& (Bitwise AND), | (Bitwise OR)

AND (to check for both conditions to be true)
SELECT * FROM student WHERE marks > 80 AND city = "Mumbai";

OR (to check for one of the conditions to be true)
SELECT * FROM student WHERE marks 90 OR city = "Mumbai";

Between (selects for a given range)
SELECT * FROM student WHERE marks BETWEEN 80 AND 90;

In (matches any value in the list)
SELECT * FROM student WHERE city IN ("Delhi", "Mumbai");

```
SELECT *
FROM student 
WHERE city IN ("Delhi" , "Mumbai" , "Gurugram");
```

![](attachments/Pasted%20image%2020260822223126.png)


```
NOT (to negate the given condition)
SELECT * FROM student WHERE city NOT IN ("Delhi", "Mumbai");
```

![](attachments/Pasted%20image%2020260822223252.png)

**Introduction to Operators**

"Now, as we have clubbed these two conditions, there are several different ways to connect two conditions together1. And here, we are going to study about our operators1. Whenever we use the WHERE clause, there are many operators that we can use along with the WHERE clause1.

What is the meaning of operators? We must have studied in math that to add two numbers, we use the plus sign (+); to multiply two numbers, we can write this sign (*); to divide two numbers, we write the division sign (/)1. So, this plus, this multiply, this divide—all of these are our operators in math1. So, the operators that are in math, similarly, similar operators are used in our SQL, and these are of four types1.

The very first operators we have are **Arithmetic Operators**2. Arithmetic, meaning those which are generally used in math, such as our plus (+) operator, minus (-), multiply (*), and divide (/)2. Along with this, there is one more, which is **Modulus**2. The job of modulus is that it tells us the remainder2. For example, if I write `4 MODULUS 2`, then its value will come equal to zero2. Why? Because this is telling me that when four is divided by two, we get two, and minus four leaves zero—so what will the remainder be? The remainder will come equal to zero2. Absolutely basic math2. So, this operator is not typically used in basic math, but in coding, generally in programming languages, it is there, and its job is to give us the remainder2.

We can test this out once2. For this, we can write that we want to print the values of all those students whose marks plus ten is greater than a hundred (`marks + 10 > 100`)23. This means, what have I written here? If we add 10 more marks to the marks, and that value goes above 100, I want the marks of those students3. So we can execute this3. Basically, these are two students: 93 + 10 will become 103, which is greater than 100; and 96 + 10 will become 106, which is greater than 1003. So, it gave me the data of all those students3. So here, which operator did we use? Our plus (+) operator3."

**Comparison Operators**

"Similarly, we have **Comparison Operators**3. The job of comparison operators is to compare things—which thing is bigger, which thing is smaller3. In this, the very first operator is equal to (=)3. In math, when I have to check if $a$ equals $b$, for that we write `a = b`; similarly, in SQL also we write `a = b`3. For example, if we write here that we only want the details of those students whose marks' value is equal to 93, then if we want, we can execute this3. So what happened? We got Bhumika's marks, whose marks are equal to 933.

Similarly, we have not equal to (!=), we have greater than (>), greater than or equal to (>=), less than (<), and less than or equal to (<=)34. For example, here we can write that give us the information of those students whose marks are greater than 90, so we know that from here we will get that correct information4."

**Logical Operators**

"Similarly, we also have some **Logical Operators**4. Among logical operators, our AND comes, OR comes, NOT comes, IN, BETWEEN, ALL, LIKE, ANY—many such operators come4. We will see them in detail shortly4. And along with that, our bitwise operators come, which help us work on bits4. So, if you have learned programming in binary, then we must have studied bitwise operators there4. Otherwise, these are a bit advanced, they won't have that much usage4. Mostly, we work with our comparison, logical, and arithmetic operators4."

**1. AND Operator [01:20:41]**

"Now first, let us study some of the important operators in detail4. The very first is our **AND operator**4. The job of the AND operator is that it checks whether both conditions are true4. AND, meaning the condition written on its left side and the condition written on its right side, both conditions must be true4. For example, if we write here:

```
SELECT * FROM student WHERE marks > 90 AND city = "Mumbai";
```

In that case, both of the conditions must be true45. Let's select this much part and execute it5. So, only one student came, whose marks are also above 90 and whose city is also Mumbai5. It is not like if someone's city is Mumbai but marks are less than 90, then they will get printed5. Both of the conditions must be satisfied when we use AND5."

**2. OR Operator [01:21:19]**

"Similarly, we have another operator, **OR**5. The job of OR is that any one of the two conditions gets satisfied5. If even one of the two returns true, if even a single condition is satisfied, then our data will come5.

So if we simply put OR in place of AND here and execute this statement, then in that case we will get: first, Bhumika came, where marks are also greater than 90 and city is also Mumbai5; second came our Chetan, whose marks are not greater than 90, but city became Mumbai5; then came Dhruv, whose marks are greater than 90, but city became Delhi56. So, he satisfied the marks condition but not the city condition6. He satisfied the city condition but not the marks condition6. And this student (Bhumika) satisfied both conditions6. So there won't be any student who doesn't satisfy either6. But yes, if any one condition is satisfied, then our OR brings us the result6."

**3. BETWEEN Operator [01:22:01]**

"Apart from this, we have another operator, our **BETWEEN operator**6. Whenever we need to select data within a range, indicating that some particular value of ours should be between a range, then we use BETWEEN6. For example, if we want the data of those students whose marks are between 80 and 90, then for that we can use6:

```
SELECT * FROM student WHERE marks BETWEEN 80 AND 90;
```

And in this, both 80 and 90 are inclusive6. Inclusive means if someone's marks are exactly 80 or someone's marks are exactly 90, their data or their value will also come to us6. So we can write: `marks...` let's remove this much part... `BETWEEN 80 AND 90`67. So let's select and execute this7. So these two students came: one has marks 85, the other has marks 827. So, this is how our BETWEEN works7."

**4. IN Operator [01:22:52]**

"Along with this, we also have an **IN operator**7. The job of the IN operator is that if after the IN operator we give any list—a whole list of values, like suppose here we gave a list in which two cities are written: Delhi and Mumbai7. In this way, we can write 10 cities also, we can write 20 cities also—so what the IN operator does is, it matches7. For example, if we wrote `SELECT * FROM student WHERE city IN ('Delhi', 'Mumbai')`, then if the value of the city is equal to any one given value from this list, then our data will come7. So we will get the data of all those students whose city is either Delhi or Mumbai7.

Right now, we are working on a database of only six students, but whenever there is a database inside a college, there can be thousands of students7. So there could be a database of 10,000 students in total, and in that, we only want the data of three nearby cities7. So in that case, we can use IN for our SQL query7.

So here we can write `WHERE city IN` and here we can give our proper list inside which we will write Delhi, inside which we will write Mumbai, so that it is within these cities7. And we can also write such a city which is not in our database at all—so let's suppose we have written Gurgaon here, so we wrote such a city which is not in the database at all7. Let's select and execute this7. So here, all of these students have come to us7.

So even if we have written something in the list that might not be in the database, still no error will come; it will simply check if there is anything from here78. If we wrote something like, let's suppose we wrote Faridabad here, and both of these cities are not in our database, then in this case, an empty table will print for us—meaning nothing will come, only null values will come, because there is no such student who lives in Faridabad or Gurgaon8. So, this is how IN works8."

**5. NOT Operator [01:24:29]**

"And right there, we have another interesting operator, **NOT**8. What does NOT do? It negates the given condition—whatever condition is given, it extracts its negative8. If we are checking IN, and we put NOT in front of it, then it will check the opposite, meaning it will pull the data of those students who are not inside these cities8. So whenever we have to reverse a condition, we put NOT there8.

For example, here we can apply... let's do Ctrl+Z... did a bit too much Ctrl+Z... let's write Delhi here, and here we write Mumbai, and here we apply `NOT IN`8. So with the help of NOT, all those students will come who do not live in these cities8. For example, one student came who lives where? He lives in Pune, he does not live in Mumbai and Delhi8. So, this is how NOT works8. So, these are our most commonly used operators, which we use with our WHERE clause8."


