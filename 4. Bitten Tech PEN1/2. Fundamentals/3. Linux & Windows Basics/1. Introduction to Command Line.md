
Command Shell Basics ▰ Text-based application for viewing, handling, and manipulating files on your computer ▰ Other names: cmd, CLI, prompt, console or terminal. ▰ User to Kernel interface ▰ Interface to use operating system services ▰ Read from input devices such as keyboards or from files ▰ The shell gets started when the user logs in or start the terminal.

![428](attachments/Pasted%20image%2020260814224653.png)

In this as well, the first thing we have is the command shell. Let's learn about its basics—what a shell actually is. So, if I talk about any operating system, many concepts come up about how an OS is built. If you have some knowledge of what the OS structure looks like from a high level, then you have hardware. If I show you a diagram, this is the diagram I have. The one in the very middle, at the center, is hardware. Everything comes under hardware—your printer, disk drive, RAM, CPU, any terminal, any USB port, NIC, or whatever you have that you can physically see, which is installed in your CPU, in your monitor, whatever is plugged in, that is your hardware. So ultimately you...

### Part 3: The Operating System, Kernel, and Shell

...have to use software so that you don't have to directly interact with the hardware. You want to get your work done from the hardware so that you don't have to program the hardware separately, which is why we use an OS. What does the OS do? It takes our commands and gives them to the kernel. The kernel gives them to the hardware.

Now, what is a kernel? If you don't know what a kernel is, I made a video on that, you can watch that too, but let me explain briefly. The kernel is the central part of the OS. I mean, it does all the work, guys. I have already explained what a kernel is; I won't discuss it again.

Now we come to the shell. Above the kernel comes the shell. Now, what is a shell, brother? Look, the kernel is above the hardware, so obviously, the kernel is directly interacting with the hardware. It manages the hardware completely. Now, who will give commands to the kernel? We will give commands to the kernel, right? Now, what is the problem here? What the kernel understands is very difficult for us to understand. That is, it understands machine language. That means, in most parts, it is machine language. I mean, it's not that low-level...

### Part 4: Privileged Mode and the Shell as an Interface

...but still, there are some commands, some system calls that we cannot remember. And the biggest thing is that it works in privileged mode. Privileged mode means that the process we are running will call a system call. And that system call will go to the kernel. The kernel will decide where to run the call, which hardware will run, and how to use that hardware. So our process will do all the work; we cannot do it directly.

So, to get the work done by that process, we have a concept here called the shell. The shell is a text-based command—it is a software. It is also a software, guys. It is system software. It comes with the OS. Whether you use Windows or Linux, it will be in both. It is of different types in both, both have different power levels, and different commands. So, the simple meaning of a shell is to connect a user's command—or simply connect a user—to the kernel. That means it is an interface from kernel to user, or user to kernel. So, the applications above the shell are our user...

### Part 5: User Applications and Shell Mechanics

...applications. They are our processes, our programs that we run. Like an email application, MS Word, some gaming software, or our text editor. All of these that we run are our applications. What do those applications do? They send commands to the shell. We can either send commands from the application or send commands directly to the shell. Okay?

The shell is not that black thing you see. That is just a medium through which we are using the shell; it is not the actual shell. What is a shell? The shell is a command interpreter. The black screen that you see is also a GUI of the shell. Actually, the shell doesn't have any visual component. So, when we want to enter commands directly from the user to the kernel, we can do it ourselves through the shell. So, this is the shell: a text-based application for viewing, handling, and manipulating files on a computer. You can do any task. If you want to get some work done by the system, from the system's hardware, like copying a file or...

### Part 6: Tasks Managed by the Shell and Different Names

...changing the name of a system file, copying a file, or checking your network IP, etc., changing the system configuration, or changing the system's password. You can do all of this from the shell. You get direct kernel access.

And why are we using the shell? Because we cannot understand what the kernel understands, right? Why are we using the shell? Because we can easily type commands in the shell, and the shell converts it. It interprets it into system calls, and the kernel understands those system calls. So, according to those system calls, it will run the hardware. System calls work in privileged mode. So, the shell also handles the task of going from user mode to privileged mode for us.

It has many names, guys. People all over the world call it CMD, CLI, command prompt, console, terminal—whatever you say, the actual meaning should be understood. These are all different names for the shell. Now, I said that this shell reads from input devices, or it can also read from a file.

### Part 7: Terminal vs. Shell and Programming in Shell

Just like you normally do in programming, it can read from standard input or from a file. This is an important point. Whenever a user logs in, a separate terminal is created for them. The shell is a common thing, brother. The shell always starts. But every user's terminal is different. When we study Unix—I mean, when we study Linux—we will understand what a terminal is and what a shell is, and the difference between the two. So, every user has a different terminal, but the shell remains the same here.

So, you type commands in the shell, and hackers use this. They do not get any separate GUI. Whatever scripts and tools we have, they are all made in programming languages, and in the shell, you can directly run any programming language, like Python, PHP, Perl, or Bash [transcribed as "maths 12"], whatever you want, you can run it in the shell. If any application is installed on the system, you can access it from the shell. So the shell is quite flexible. We get direct access to the system kernel...

### Part 8: Shell Privileges and OS Variations

...if we have the privilege. So we use this a lot. Hackers use this. So it is very important to know how the shell works. I hope you have understood.

The shell looks something like this. If you can relate to this, you might have seen it in Windows. We will look at Linux's shell too, even if you haven't seen it. So, we type these common commands here. Here we have a listing of which user is currently active, which user you are logged in as, and what our system's name is. This is different in Linux and different in Windows.

