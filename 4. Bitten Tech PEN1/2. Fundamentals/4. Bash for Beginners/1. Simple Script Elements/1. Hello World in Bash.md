
```
#!/bin/bash

echo "Hello world"
```

```
└─$ ls -la             
total 12
drwxrwxr-x  2 kali kali 4096 Aug 16 04:54 .
drwx------ 23 kali kali 4096 Aug 16 04:40 ..
-rw-rw-r--  1 kali kali   31 Aug 16 04:54 new.sh
```


```
└─$ bash new.sh 
Hello world
```

```
└─$ ./new.sh 
zsh: permission denied: ./new.sh

```

```
┌──(kali㉿kali)-[~/bash-scripts]
└─$ chmod +x new.sh 
                                                                                                
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ls -la
total 12
drwxrwxr-x  2 kali kali 4096 Aug 16 04:54 .
drwx------ 23 kali kali 4096 Aug 16 04:40 ..
-rwxrwxr-x  1 kali kali   31 Aug 16 04:54 new.sh

```


```
┌──(kali㉿kali)-[~/bash-scripts]
└─$ chmod -x new.sh
                                                                                                
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ls -la         
total 12
drwxrwxr-x  2 kali kali 4096 Aug 16 04:54 .
drwx------ 23 kali kali 4096 Aug 16 04:40 ..
-rw-rw-r--  1 kali kali   31 Aug 16 04:54 new.sh

```

```
└─$ chmod u+x new.sh
                                                                                                
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ls -la          
total 12
drwxrwxr-x  2 kali kali 4096 Aug 16 04:54 .
drwx------ 23 kali kali 4096 Aug 16 04:40 ..
-rwxrw-r--  1 kali kali   31 Aug 16 04:54 new.sh

```

```
└─$ ./new.sh
Hello world

```


### art 1: Getting Started with Bash Scripting & Setting Up VS Code

Hello friends. So from today we will start doing Bash scripting [बैश स्क्रिप्टिंग]. Today we will make our very first Bash script. The simplest script which you must have heard of—we will make a Hello World program.

So we are going to use Visual Studio Code here. So you ensure that it is installed in your system and then you are good to go. So now first we will come to that directory where you have to save your code. I recommend this: that you make a separate directory for all the files that we are going to read throughout this entire course. Many files will be made. Make that separate directory, and in that directory, you have to initialize Visual Studio and then simply you have to start by writing code. And after that, the name of the directory, meaning where you want to initialize it. Where will all the files you make in it be stored? So my current directory is where I have to do it. So I type dot (`.`) here and hit enter. So here your Studio Code will open. And its font, let's make it bigger now. See this...

### Part 2: Writing the First Script & The Shebang Concept

...here it has become Bash. Inside this, you can make whatever files, folders, etc. you want to make. So now what will we do? We will make a new file. Here we go to start, new file. And here I will show you by making it bigger. You can press Control Plus. So it means, in that way it will become bigger. It is not a big deal.

I will keep this on the right—and the terminal will be, sorry, the terminal will be on the right and here will be my Visual Studio. You can close this if you want in this way. So now let's start writing our Bash code here. Right now it won't understand our language because it doesn't have any extension yet, but how a simple Bash code is written, meaning what its format is, you will understand that here now.

So now what I do is, I write a simple line. First of all, pay attention here, I will write a comment, so the comment starts with a hash (`#`) like this here in Bash. Okay? A single line comment. After that, I will write this exclamation mark (`!`). Okay? This combination of hash and exclamation, this is called Shebang [शी बैंग]...

### Part 3: Role of the Shebang and Binary Paths

...called. This "She"—meaning "She" came from hash (`#`), and the "Bang"—and this "Bang" has come from this, you can call it exclamation (`!`). So these two are a combination. This is, like, a special directive. This will always remain in, like, the first line of your script. Okay?

After that, you will give a path here. Whose path will it be? It will be of your binary. Meaning, the binary by which you want to interpret this code. You will give its path here. So why is that necessary? Okay, I will tell you later. First, I will write `#!/bin/bash` [transcribed as 'Ben bash' / 'ben bash'] here. Because I am making a Bash script, so I will give `#!/bin/bash` here.

If you were making a Python script, you would give, like, where your Python is installed—`/usr/bin/python` or wherever your Python 2, Python 3 is installed, or if you have setup an ENV, then it will be inside the ENV. You will give its full path. After that, then you will start writing whatever code you have, so this should be in the first line. After that, you can write whatever you want to write.

So now, what do I have to write? Simply, I have to write Hello World. So I told you that `echo`...

### Part 4: The `echo` Command, Saving, and Permissions

...is a command. So just like you were writing commands in the terminal, you have to write here as well. There is no difference. There is additional functionality here which we will read later.

So after `echo` [transcribed as 'ego'/'एगो'], we have to write "Hello World". Uh, that's it, and this is our simple script. And nothing else has to be done. Our work is finished. Just save it. Control + S, and then it will be saved exactly where you wanted, so I will name it 'first script' or let's write it as 'hello_world' because it feels good, and I will store it because it will highlight the syntax and all, so look, it has highlighted this `#!/bin/bash` [transcribed as 'ben bash' / 'बन मैश'].

So what is the meaning of this, let's understand here now. So I do `ls`. Now see here, `hello_world` has come. Its permissions—let's see what permissions are there right now: read-write, read-write. Here, if I want to execute it, I cannot. Now how will I execute it? I told you that with the `bash` binary, you can do it. So I write `bash`, and then the name of this Bash script, `hello_world`. What happens? Look, it got executed.

### Part 5: Executing Scripts (`./`) and Permission Modifications (`chmod`)

...but now here it got executed because I ran it with the `bash` command. But there is another way to execute any script, which is dot-slash (`./`). Dot-slash, and after that, the name of that file. Look, here it is not executing.

If someone wants to do this—like others, like group—if I want to give it permission to execute, then for that, I have to put 'x' here. I have to put 'x' here. So it does not have executable permission. So how will I give that to it? So first I will have to give that, only then will I be able to execute it like this.

So now what do I do? I do `chmod` [transcribed as 'सीए मोड' / 'ca mod']. It doesn't have it. To whom should I give permission? I won't give it to everyone here. If I write `chmod +x hello_world` here, what will it do? See, it gave it to everyone. It gave executable permission to the user, to the group, and to others as well. So I don't want this. For me, what is it—I will first do minus (`-x`) here, so everyone's permission must have been removed. Okay? I only want it for the user, so that only...

### Part 6: Restricting Permissions to Owner (`chmod u+x`) and Extension Flexibility

...only its owner can execute it. So for that, I will write `u`. `u` means owner. `g` means group. `u+x` [user + execute]. Okay? So do not make that mistake. Many people just write `+x` followed by the file name. It's done. Now it is correct. Only the owner will be able to execute this.

Now run it here: `./hello_world`. Okay. So this is your first script, it has run. How did it run? Here I have only written dot-slash (`./`).

Now in this way, whether you take a Python file, or any ELF file, compiled binary, or any executable file, a file with any extension, or whether there is an extension or not. There was no need to write `.sh` here. Even without the dot, it would have worked. It would have run even without an extension. You try it as-is.

Still, how did it know which interpreter to use? This `.sh` which is here, that is there. But I can remove it too. If the name was only `hello_world`, how would it know whether it is a Python script, a Bash script, or a Ruby script? Actually, I...

### Part 7: Script Interpreter Behavior and Best Practices

...have told it. Where have I told it? See here, this line that you see tells it. Meaning, when your shell reads this script, it will see this in the first line and then it will understand whom it is made for, where it has to be interpreted. So this will load the binary and then run the rest of the script against it and then execute it. And only then will your script be able to run properly.

So that is why this is important—I mean, I could also run it with `bash`, I could do it with `python`, then I wouldn't need this Shebang, but if I do dot-slash (`./`) here, then I need it. That is why it is recommended to write this.

I will remove it from here. Okay? I won't write it right now. Now I will run it. Okay? It ran because by default, meaning many times, like, often it happens that it knows what its behavior is and how to run it. `echo Hello World`—and, like, most of the time, `echo` is used similarly in many programming languages too.

### Part 8: Multi-Language Shebang Verification (Bash vs. Python)

But if you write complex scripts or, like, a not-so-popular type of syntax, then you will have to give the Shebang there. And the good practice is exactly that—if I wrote here, for example, `/usr/bin/python`, if I tell it that this is a Python script, then what would it do? Look here: 'bad interpreter'. So this means, it is clear from here that it is reading this Shebang: 'bad interpreter'.

So the script didn't run at all—'No such file or directory'—because `echo` does not work in Python, that is why. Okay, so this was our first script, so that's why I am explaining the basics to you. Next time onwards, keep this in mind here, and the further work, we will see in the next video. So this was a simple Hello World script, and that's all for today. Keep watching...

