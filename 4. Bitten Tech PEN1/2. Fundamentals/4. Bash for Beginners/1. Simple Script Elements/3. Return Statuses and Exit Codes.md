
```
#!/bin/bash

name=$(who)

echo "Hello $name "
echo $?
```

```
└─$ ./new.sh
Hello kali     seat0        2026-08-21 02:13 (:0) 
0

```



```
#!/bin/bash

name=$(wh)

echo "Hello $name "
echo $?
```

```
└─$ ./new.sh
./new.sh: line 3: wh: command not found
Hello  
0

```




```
#!/bin/bash

name=$(wh)
echo $?

echo "Hello $name "
echo $?
```

```
└─$ ./new.sh
./new.sh: line 3: wh: command not found
127
Hello  
0

```

```
#!/bin/bash

name=$(who -y)
echo $?

echo "Hello $name "
```

```
└─$ ./new.sh
who: invalid option -- 'y'
Try 'who --help' for more information.
1
Hello  

```



```
#!/bin/bash

name=$(who -y)
echo $?

echo "Hello $name "
echo exit 5
echo exit 0
```

```
└─$ ./new.sh
who: invalid option -- 'y'
Try 'who --help' for more information.
1
Hello  
exit 5
exit 0

```


### Part 1: What are Exit Statuses and Exit Codes?

Hello friends, in this video, we will see what exit statuses are. Hello friends, in this video, we will see what exit statuses are, and what exit codes are.

So a little bit of background is that whenever you run any command in Unix, it runs as a process, and it has a process ID. So that process must have been called by something—by your shell process. Hello friends, in this video we will see who it calls, what it does, why it does it, and how it will be useful to us. Let's see that in this video.

So a little bit of background is that whenever you run any command in Unix, it runs as a process. It has a process ID. So that process must have been called by someone. Your shell process has called that other command.

So the parent process that you have, you need to return some exit status to it whenever your command or your process completes or finishes its execution. So in short, any process in Unix, after executing, returns an exit status or exit code to its parent process so that the parent can know whether your program ran successfully or if some error occurred, or what type of error occurred. All of this data goes to the parent.

### Part 2: Understanding Exit Codes (0 to 255) and Error Detection

So if there is any problem in your program, you can detect it using this exit status, just like exception handling in programming. Here, it is something very similar. If you find out the exit code, it will tell you what the problem is in your program, or if there even is a problem or not.

So there are many codes. In Unix, there are exit codes from 0 to 255, and every code has its own specific meaning. For example, if you typed a command incorrectly, it has a different code, or if you provided an incorrect option, it has another different code. Or if some other parameter was wrong, or simply if the program doesn't run properly, it gives a different code.

And if your program ran correctly, executed completely to the end of the file, and no error occurred, then it gives exit code 0. This is a special exit code. Except for 0, if it gives any other code, it means the program did not run correctly. Okay? So keep in mind that you should keep your program's exit code as 0 unless you specify otherwise explicitly.

### Part 3: Accessing Exit Codes in Shell Scripts and the `exit 0` Practice

So how can you access your program's exit code in your shell script? You might have seen in many scripts that at the end, we write `exit 0`.

It gives exit code 0. That is a special exit code. Any code other than 0 means the process did not run correctly. So, your parent process needs to return some exit status when your command or process completes or finishes execution.

So in short, any process in Unix, after executing, returns an exit status or exit code to its parent process so that the parent can know... it successfully executed. But this is not the correct way. Because whenever and however you execute it, the exit code will always go as 0, whether your program ran correctly or not. So this is not a proper practice, although people still write it. This `exit 0` has a literal meaning that I want to return 0. So it will run as is. It won't make any difference to the program's execution.

But if you want to find the exit code of any command in your script or find its return code, there is a value for it—meaning, to access it, you use `$?` (dollar question mark). This question mark is a variable that we are accessing using the dollar sign, and it tells you the exit code of your last command.

### Part 4: The `$?` Variable and Error Checking

What is the last command in this case? It is `echo`. So there won't be any problem here because this is 'hello', 'name', and 'name' has been declared. There won't be any problem in this.

So now what we do is, we don't exit this, we echo it to see what exit code it is giving us. Let me run this. Look, it will show... okay, see, 0 came. 0 means successful. There was no problem at all in this command. There was no problem in this command. Meaning, whenever you access this value, it holds the exit status of the command immediately preceding it. Okay? So whenever you run a new command, its value will change.

So you have to check this here. Here, I have put a command. If I make a mistake here, like if I remove 'o' [from a command like echo]. Now, this is not a valid command in Unix, so it will give an error. Okay? See, it is showing 'command not found'. But still, the exit status is 0 [because the error check wasn't run immediately after the failed command, and instead ran after a successful one]. That's why I said that it contains the exit code of the last command. So if we want to check for an error, we must do it immediately after the command. Whichever command you are running and you suspect there might be an error...

### Part 5: Error Codes and Practical Use Cases (e.g., 127, 1, and `grep`)

...then you should echo it right there. Now let's see what comes. See, 127. 127 means your command was not found; it didn't understand it. So this is a different exit code.

And if you want to end your script with the exit code of your last command, you can write `exit $?`. Writing `exit $?` is the standard way that people do. This is written at the very end of your script, when everything has finished executing.

Okay, here is `exit`. I will echo again. Here I write an invalid option. The command is correct, but the option is invalid. Then what does it give? See, it is giving 1. This means the exit code for an invalid option is 1.

Now let's see what comes. See, I write `exit 5` here. So the program exited here. Even though you had written `exit 0` at the very end, your exit code will still be 5 because the program exited before reaching that. The parent process will always get 5.

So these are exit codes and return values. Now I hope you understand them. In many cases, this might not seem useful to you. But in many cases, you will need to find out whether your program or command ran successfully or not, and then you can use this. Or if there is a use case where you are taking a string input from the user and checking in a file whether that string exists or not. In that case, `grep` [transcribed phonetically as "gap"], if it doesn't match any pattern, returns a different exit code, and if the input file itself is not there, it will give another exit code. In this way, if you want to find out what type of error occurred, it is very useful here with this help.

