
```
└─$ seq 1 5
1
2
3
4
5

```


IMP 
**you cannot put spaces around `=`**.


```
#!/bin/bash

name=$(seq 1 5)
echo $?

echo "Hello $name"
```

```
0
Hello 1
2
3
4
5

```

```
#!/bin/bash

name=$(seq 1 5)
echo $?

echo Hello $name
```

```
0
Hello 1 2 3 4 5

```


```
#!/bin/bash

name=$(seq 1 5)
mkdir $name

ls
```

```
└─$ ./new.sh
1  2  3  4  5  new.sh
                                                                                                        
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ls
1  2  3  4  5  new.sh

```




```shell
└─$ printenv       
COLORTERM=truecolor
DISPLAY=:0.0
LANG=en_US.UTF-8
LANGUAGE=
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/local/games:/usr/games
TERM=xterm-256color
XAUTHORITY=/home/kali/.Xauthority
XDG_CURRENT_DESKTOP=XFCE
LS_COLORS=rs=0:di=01;34:ln=01;36:mh=00:pi=40;33:so=01;35:do=01;35:bd=40;33;01:cd=40;33;01:or=40;31;01:mi=00:su=37;41:sg=30;43:ca=00:tw=30;42:ow=34;42:st=37;44:ex=01;32:*.7z=01;31:*.ace=01;31:*.alz=01;31:*.apk=01;31:*.arc=01;31:*.arj=01;31:*.bz=01;31:*.bz2=01;31:*.cab=01;31:*.cpio=01;31:*.crate=01;31:*.deb=01;31:*.drpm=01;31:*.dwm=01;31:*.dz=01;31:*.ear=01;31:*.egg=01;31:*.esd=01;31:*.gz=01;31:*.jar=01;31:*.lha=01;31:*.lrz=01;31:*.lz=01;31:*.lz4=01;31:*.lzh=01;31:*.lzma=01;31:*.lzo=01;31:*.pyz=01;31:*.rar=01;31:*.rpm=01;31:*.rz=01;31:*.sar=01;31:*.swm=01;31:*.t7z=01;31:*.tar=01;31:*.taz=01;31:*.tbz=01;31:*.tbz2=01;31:*.tgz=01;31:*.tlz=01;31:*.txz=01;31:*.tz=01;31:*.tzo=01;31:*.tzst=01;31:*.udeb=01;31:*.war=01;31:*.whl=01;31:*.wim=01;31:*.xz=01;31:*.z=01;31:*.zip=01;31:*.zoo=01;31:*.zst=01;31:*.avif=01;35:*.jpg=01;35:*.jpeg=01;35:*.jxl=01;35:*.mjpg=01;35:*.mjpeg=01;35:*.gif=01;35:*.bmp=01;35:*.pbm=01;35:*.pgm=01;35:*.ppm=01;35:*.tga=01;35:*.xbm=01;35:*.xpm=01;35:*.tif=01;35:*.tiff=01;35:*.png=01;35:*.svg=01;35:*.svgz=01;35:*.mng=01;35:*.pcx=01;35:*.mov=01;35:*.mpg=01;35:*.mpeg=01;35:*.m2v=01;35:*.mkv=01;35:*.webm=01;35:*.webp=01;35:*.ogm=01;35:*.mp4=01;35:*.m4v=01;35:*.mp4v=01;35:*.vob=01;35:*.qt=01;35:*.nuv=01;35:*.wmv=01;35:*.asf=01;35:*.rm=01;35:*.rmvb=01;35:*.flc=01;35:*.avi=01;35:*.fli=01;35:*.flv=01;35:*.gl=01;35:*.dl=01;35:*.xcf=01;35:*.xwd=01;35:*.yuv=01;35:*.cgm=01;35:*.emf=01;35:*.ogv=01;35:*.ogx=01;35:*.aac=00;36:*.au=00;36:*.flac=00;36:*.m4a=00;36:*.mid=00;36:*.midi=00;36:*.mka=00;36:*.mp3=00;36:*.mpc=00;36:*.ogg=00;36:*.ra=00;36:*.wav=00;36:*.oga=00;36:*.opus=00;36:*.spx=00;36:*.xspf=00;36:*~=00;90:*#=00;90:*.bak=00;90:*.crdownload=00;90:*.dpkg-dist=00;90:*.dpkg-new=00;90:*.dpkg-old=00;90:*.dpkg-tmp=00;90:*.old=00;90:*.orig=00;90:*.part=00;90:*.rej=00;90:*.rpmnew=00;90:*.rpmorig=00;90:*.rpmsave=00;90:*.swp=00;90:*.tmp=00;90:*.ucf-dist=00;90:*.ucf-new=00;90:*.ucf-old=00;90::ow=30;44:
MAIL=/var/mail/kali
LOGNAME=kali
USER=kali
HOME=/home/kali
SHELL=/usr/bin/zsh
SUDO_COMMAND=/usr/bin/su
SUDO_USER=kali
SUDO_UID=1000
SUDO_GID=1000
SUDO_HOME=/home/kali
SUDO_TTY=/dev/pts/4
COMMAND_NOT_FOUND_INSTALL_PROMPT=1
POWERSHELL_UPDATECHECK=Off
POWERSHELL_TELEMETRY_OPTOUT=1
DOTNET_CLI_TELEMETRY_OPTOUT=1
SHLVL=2
PWD=/home/kali/bash-scripts
OLDPWD=/home/kali
LESS_TERMCAP_mb=
LESS_TERMCAP_md=                                                                                        
LESS_TERMCAP_me=                                                                                        
LESS_TERMCAP_so=
LESS_TERMCAP_se=                                                                                        
LESS_TERMCAP_us=
LESS_TERMCAP_ue=                                                                                        
MANROFFOPT=-c
_=/usr/bin/printenv

```

```
#!/bin/bash

echo $PATH

```

```
└─$ ./new.sh  
/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/local/games:/usr/games

```


```
#!/bin/bash

NAME="Akshit"
number=12345

echo $number + 10 

```

```
12345 + 10

```

```
#!/bin/bash

NAME="Akshit"
number=12345

result=$(( number+10 ))

echo $result

```

```
12355

```

```
#!/bin/bash

NAME="Akshit"
number1=12345
number2=67890

result=$((number1+number2))

echo $result

```

```
80235
```




```
#!/bin/bash

NAME="Akshit"
number1=12345
number2=67890

result=`expr $number1 + $number2`

echo $result

```

```
80235

```

```
#!/bin/bash

NAME="Akshit"


#result=`expr $number1 + $number2`

echo $1



```

```
└─$ ./new.sh 12345 567
12345

```

```
#!/bin/bash

NAME="Akshit"


#result=`expr $number1 + $number2`

echo $2

```

```
└─$ ./new.sh 12345 567
567

```

```
#!/bin/bash

NAME="Akshit"


#result=`expr $number1 + $number2`

echo $0

```

```
└─$ ./new.sh 12345 567
./new.sh

```

```
#!/bin/bash

NAME="Akshit"


#result=`expr $number1 + $number2`

echo $#
```

```
└─$ ./new.sh 12345 567
2

```

```
#!/bin/bash

NAME="Akshit"


#result=`expr $number1 + $number2`

echo $*

```

```
└─$ ./new.sh 12345 567 123456
12345 567 123456

```


```
#!/bin/bash

NAME="Akshit"


result=`expr $1 + $2`

echo $result


```

```
└─$ ./new.sh 10 20 
30

```

```
#!/bin/bash

NAME="Hello"
SURNAME="World"

result=`expr $1 + $2`

echo $NAME $SURNAME

```

```
└─$ ./new.sh       
expr: syntax error: missing argument after ‘+’
Hello World

```

```
#!/bin/bash

NAME="Hello"
SURNAME="World"

result=`expr $1 + $2`

echo ${NAME}ISTRUE

```

```
HelloISTRUE

```



```
#!/bin/bash

NAME="Hello"
SURNAME="World"

#result=`expr $1 + $2`

echo "My name is $NAME $SURNAME and my username is $USER and my hostname is $HOSTNAME, my home directory is $HOME and my shell is $SHELL and it's version is $BASH_VERSION"


```

```
└─$ ./new.sh
My name is Hello World and my username is kali and my hostname is kali, my home directory is /home/kali and my shell is /usr/bin/zsh and it's version is 5.3.9(1)-release

```

**[Section 1: Introduction to Variables and Command Substitution]**

**Hello friends, in this video we will talk about variables—how variables work in Bash, what types they are, and how they are used**. I have already told you about the introduction to variables, how to declare and print them, right? So here in this video, we will move forward with that. How are variables declared? I have already declared them here before. And here, **I have also done command substitution**. The special thing about command substitution is that **it removes any newlines from the output**. For example, here I issue a command named sequence, **seq**. What it does is print the sequence of numbers from where you specify to where you specify in a newline, as I will show you here: **seq 1 5**—so it will print the entire 1 to 5, and it will do it in a newline, meaning every time it will change the line. **But here, if you put it in command substitution, it means that all newlines will be removed**.

**[Section 2: Handling Newlines with and without Double Quotes]**

...and all these numbers will come in a single line. So, if I show you by saving this, now let's run it. Hello World. Okay? Here you can see that it is retaining the newline. Here, I said that newlines are deleted. Meaning, did I say it wrong here? But it is not like that. **Note one thing here—you have used double quotes**. **If you use double quotes, it will retain the newline**. But if double quotes are not used here... okay? **If you print it like this, then see here, all newlines are removed**. And it does not remove non-trailing newlines; it only removes trailing newlines. So that's why, whenever you grep or do some processing like that in your input, you have to keep this in mind, and it will be substituted here. For example, you have to make many directories depending upon the output of a certain command—so looking at its output, you have to make directories, you can do that. I will remove this here. Here I...

**[Section 3: Creating Directories and System vs. User Defined Variables]**

...do **mkdir output_of_name**. What will be the output of name? See here, there are no double quotes, so they will all come on the same line. This is the kind of output that will come. After that, what to echo? After that, I don't have to echo. I will do **ls**. Okay? I will do a simple `ls`. Let's see if these directories are being created or not according to its output. Meaning, 1, 2, 3, 4, 5—five directories should be created. So see, 1, 2, 3, 4, 5, Hello World. Now let's do `ls` again here for our satisfaction. See here, all five directories have been created. So in this way, you can issue another command from the output of one command; I can say that. So these are simple variables. Now let's talk about types of variables. **Types of variables—meaning, the variable we just created named 'name' is one we made ourselves, in our script**. **There are many such variables that are predefined, which are called system-defined or environment variables, about which you must know, like PATH, SHELL... there are many**.

**[Section 4: Environment Variables and Name Conventions]**

So, if you want to print which variables are in your current environment and what their values are, there is a command for that: **printenv**. There are other commands too, like `env`, `set`, and they all have different tasks. **But you will find most of the variables you need right here with this command**. So, press enter. See here, all the variables that are set and their values for the current shell are visible to you here. Shell is `/bin/bash`, color term is `truecolor`. Coming down, log name is also given, `tech_hacker`. Log in is... home is for the home directory, username is... and meaning, these variables are set; you won't have to set them. You can directly echo them in your shell script. **echo $PATH**—that's all I did, and here I ran it, and whatever PATH is, it will be printed. So their values are already defined. So whatever you want to run or use in your script, you can use it. Now let's talk a bit about user-defined variables. **Meaning, what is the naming convention for variables?** There is some naming convention too, right? I can't just directly write 1 2 3 4 equal to some value. A variable's name is not defined like that. There are some conventions for it.

**[Section 5: Variable Naming Conventions & Case Sensitivity]**

So let me tell you, **you can start a variable name with an alphabet or an underscore**. This underscore, or any capital letter, or any small letter—you can start it anyway. **Just, it should not start with a number**. So meaning, it can be uppercase, it can be lowercase. **But keep in mind that variable names are case-sensitive**. So small 'a' and capital 'A' will be different variables. So whatever name you give, you can write it with an underscore in the name, or write it as 'dash name', or capital 'NAME'—this can also be small 'name' and capital 'NAME'. Any name can be there according to this, and whatever value you want to give, just write equal to here. 1... here I am writing 'name', so I write my name here, and write another variable 'number' which will have the value 1 2 3 4 5. Okay, so see here, for 'name' I...

**[Section 6: Untyped Languages & Simple Arithmetic]**

...gave a string here, and for 'number' I gave an integer here. So how will it know which one is a string and which one is an integer? **I told you that there is no need to specify data types in Bash**. **Data types have no meaning in Bash because this language is untyped**. Meaning, just like in Python, you don't need to specify it here; whatever value you give, it assumes its data type accordingly. So here it's a string, so 'name' will be a string, and 'number' will be an integer; it decides by itself. So you know how to print them. Now let me show you how arithmetic operations are done on numbers. Like, I have taken this one number here. Now if I want to add something to it, or subtract, how will I do it? First, I'll echo. Now, how to do arithmetic operations? I will write 'number'. If I do the value of 'number' here with a dollar and do plus 10, will it evaluate? Let's see. Hello World. See, it printed as-is...

**[Section 7: Syntax for Arithmetic Evaluation (Double Parentheses)]**

...this printed as-is, it did not evaluate. Now, what are the ways we have to evaluate this thing? Now, leave that. I will declare another variable named 'result' in which I will store the output and echo it. So now, how do I do arithmetic operations? Even if I do that thing here, it will literally print, there is no point. There is a way, just like you used symbols in command substitution—dollar, and then these brackets. There is a small change here. **Here, if you want to do an arithmetic operation or calculation, you have to use double parentheses:** **$((...))**. **Double parentheses, then a space, and between these spaces, you have to write your variable 'number' and whatever you want to add—meaning plus or subtract**. I am doing plus 10, so you have to write it like this. This is its syntax for arithmetic operations, and right now I am echoing it, so let's see what comes. Here, look: 1 2 3 5—so this is calculated. Now, pay attention here, I have given spaces and stuff. So, if I remove these spaces...

**[Section 8: Variable Referencing inside Parentheses &** **expr** **Command]**

...I said that Bash is case and space-sensitive. But here, it is fine inside it, however you write it, it will work whether you give space or not. **But pay attention here, inside this, I have not given a dollar to reference 'number' because the dollar is already given outside**. **So you don't have to give a dollar inside**. Okay? Keep this in mind. And if you have to add two numbers, like this is number one and I write number two here and its value is 6 7 8 9 0, and here I do number one plus number two, and now let's see what the result is. See, this is added. So in this way, by taking input from the user, you can add two numbers, subtract them, multiply, divide—whatever you want, you can do. It works like this too, you just have to read it there, store their values in number one and number two, and perform this exact same thing. **There is another way to do this:** **expr** **is a command in Bash**. If you use `expr`, its syntax is a bit different. **For that, you will have to use backticks**. These two backticks...

**[Section 9: Arithmetic with** **expr** **& Command-Line Arguments]**

...inside them, you will run the Bash command, **expr**. This is a Bash command; you can run it in the shell too. `expr` and then, after that, giving the dollar sign, the variable name, and whatever operation you want to perform, write it. This is a second way. Run this as well. See here, the same output will come. So there are two ways. **Either you use double parentheses with a dollar, or you can use** **expr** **with backticks**. Pay attention here, the whitespaces should be exactly as I am showing. And this is one way. Now there are other ways. Here, I can read number one and number two from the user. **But command-line arguments are also a thing, right?** Which you might have studied before. Here, I don't need to write number one and number two; I will remove them. What do I do? I want to take input from the command line. Whatever two numbers I give in the command line—meaning, with my shell script when I execute it—and then I want the output of those two numbers added. So how...

**[Section 10: Accessing Arguments with Indices (Dollar 1, Dollar 2)]**

...will we do that? **You can access command-line outputs in a Bash script with the dollar sign, depending upon the index of the variable**. **Like** **$1****,** **$2****,** **$3****,** **$4****,** **$5**—whichever inputs you give in the command line, whichever arguments you give, you will get those values to access accordingly. I will show you an example. First, let's leave the result for now. I will echo `$1` here. You will find out what `$1` is. So, I am running it simply as-is without giving any argument. Syntax error. Okay? I will comment out this line for now; I don't need it. See, nothing is printed because I haven't given any argument. If I give an argument: `1 2 3 4 5`—see, 1 2 3 4 5 is printed. **So this first argument, where did its value go? In** **$1**. In this way, you will give a second argument. Okay. `1 2 3 4 5` space `5 6 7 8 9`—so this will go into the second argument. So I will have to echo `$2`. See, 5 6 7 8 9. So whichever arguments you give...

**[Section 11: Special Variables (Dollar 0, Dollar Hash, Dollar Asterisk)]**

...they will go into `$3`, `$4` depending upon the index you want to access, you can do it. **This... this is in** **$0**. If I do `$0`, see, the script name with which you executed it, its value also comes here from `$0`. And there is also an option if you want to find out how many arguments the user has given, which is also very important many times—for that, **you have the method:** **$#**. **From** **$#****, you find out how many arguments have been input**, like here there are two; if I give more space and do more, then here there are three. In this way, whatever you give, it will show it accordingly. So meaning, you have to calculate, run a loop—you can do all of this. And after that, there is a dollar... **meaning, there is also** **$***. Let's see what its job is. See, **this is an array**. **In this, all of them—meaning in a single variable, all of your arguments get stored**. So this is a matter for later; I haven't told you about that yet, how to work with arrays. So for now...

**[Section 12: Arithmetic via Arguments & String Concatenation]**

...let's not look at this. Okay? What was our job? That I want to perform an arithmetic operation depending upon command-line arguments. So I will do the same thing here: `$1` plus `$2`. And I will echo 'result' here. Okay? So now let's see, I enter both numbers: 10 and 20. See, the output is 30. So in this way, you can take from the command line. **And after that, if you want to concatenate output**, I had also told you that, like 'name' is this, and after that, if I want to enter a surname, and if I want to concatenate both of them—**in simple Bash, there is no plus symbol or dot symbol**. **You just simply write their names as-is; it will concatenate that way**. I won't give arguments. See. Okay, syntax is fine. Now see here, this has joined, and if I give a space in between, a space will come. So there's no issue, it's simple. There is another thing here, if I want to join something closely with my name, like 'is_hero'—I want to print 'is_hero' literally.

**[Section 13: Distinguishing Variables with Curly Braces]**

But I want the value up to 'name' from the dollar. But how will Bash understand where the variable reference should end? For it, the entire 'name_is_hero' is a variable. So I have to distinguish what is literal and what is a variable. **For that, you can use this symbol: curly braces** **{}**. **These are curly braces, and this will go into your variable whose value will be referenced here**. The rest of this will be printed literally. So now, see this: 'ansh is hero'. So in this way, by joining them, you can also distinguish what is a variable and what is not. So these are simple variable operations. Here, javatpoint is a good resource that tells you the meaning of command-line argument variables. Here will be the script's name, here are the arguments you entered, here is the total count of arguments, here by joining them together—it stores all command-line arguments. This is array `@`. I said asterisk earlier, sorry, but here `@` with dollar is the array...

**[Section 14: Environment Variable Printing & Bash Scripting Output]**

...on which we will work later. 'Specify the process id.' Here, this looks a bit wrong because I told you that this is the exit status. Let's confirm it once. After that, this 'dollar dollar' is saying exit status—so I think they have been interchanged here. **This... is the exit status, and this... is your process ID**. After that, 'shows ID of the last background job'—okay, that's a good thing, let's try it out once here, let's do some work here. My name, surname, okay. So I echo inside double quotes, let's get a nice output here: 'My name is [name] and my surname is... sorry, surname will come right along with it, surname, and my username is...'—meaning, from the environment variables, let's extract their values—'and my hostname is [hostname], my home directory is [home], and my shell is... sorry, [shell], and its version is [dash version].' These are all environment variables, and...

**[Section 15: Execution Verification and Final Summary]**

...the other two, 'name' and 'surname', I have declared. So let's see its output once finally. Okay, so there is a syntax error. I will remove this for now; I don't need this. Why is the syntax error coming? 'My name is Sanju Bhawana. My username is tech_hacker. My hostname is... My home directory is this. And Bash version is this, your 5.0 one.' So this is how you can use whatever variables there are, you can evaluate expressions, calculate whatever you want to calculate, and more or less, this is what happens. In future videos, it will be even clearer when we use these variables for different tasks—in loops, in arrays. So many things are left. For now, just practice as much as I have explained. Rest we will see later. Okay? So that's all for this video. Keep watching, keep learning, and goodbye.