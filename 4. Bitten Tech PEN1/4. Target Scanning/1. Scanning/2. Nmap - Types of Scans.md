
```
SCAN TECHNIQUES:
  -sS/sT/sA/sW/sM: TCP SYN/Connect()/ACK/Window/Maimon scans
  -sU: UDP Scan
  -sN/sF/sX: TCP Null, FIN, and Xmas scans
  --scanflags <flags>: Customize TCP scan flags
  -sI <zombie host[:probeport]>: Idle scan
  -sY/sZ: SCTP INIT/COOKIE-ECHO scans
  -sO: IP protocol scan
  -b <FTP relay host>: FTP bounce scan

```

```
nmap 192.168.60.138 -sT 
```

```
nmap 192.168.60.138 -sS
```

```
nmap 192.168.60.138 -sA
```

```
nmap 192.168.60.138 -sU
```

```
nmap 192.168.60.138 -sN
```

```
└─# nmap 192.168.60.138 -sN 
Starting Nmap 7.99 ( https://nmap.org ) at 2026-09-04 04:26 -0400
Nmap scan report for 192.168.60.138
Host is up (0.00030s latency).
Not shown: 977 closed tcp ports (reset)
PORT     STATE         SERVICE
21/tcp   open|filtered ftp
22/tcp   open|filtered ssh
23/tcp   open|filtered telnet
25/tcp   open|filtered smtp
53/tcp   open|filtered domain
80/tcp   open|filtered http
111/tcp  open|filtered rpcbind
139/tcp  open|filtered netbios-ssn
445/tcp  open|filtered microsoft-ds
512/tcp  open|filtered exec
513/tcp  open|filtered login
514/tcp  open|filtered shell
1099/tcp open|filtered rmiregistry
1524/tcp open|filtered ingreslock
2049/tcp open|filtered nfs
2121/tcp open|filtered ccproxy-ftp
3306/tcp open|filtered mysql
5432/tcp open|filtered postgresql
5900/tcp open|filtered vnc
6000/tcp open|filtered X11
6667/tcp open|filtered irc
8009/tcp open|filtered ajp13
8180/tcp open|filtered unknown
MAC Address: 00:0C:29:06:33:26 (VMware)

Nmap done: 1 IP address (1 host up) scanned in 1.98 seconds

```

```
nmap 192.168.60.138 -sX

```

**[Section 1: Introduction and Scan Selection]** "Not the most common, but I mean, what we do to bypass firewalls or something like that—we adopt different methods, or to optimize the scan and make it fast, we run different types of scans: TCP scan, UDP scan. So okay, let's see here. In Nmap's help, you will get all of those things that I am telling you here. Okay? So you can see these options by going up. If we go a little further up, you will find all of this in scan techniques. So, first of all, we are going to do the **TCP SYN scan**, which was the complete connect scan, right? I mean, the one that was giving us output. They call it `sS` maybe, `-sT` was for full scan and TCP SYN scan was for `-sS`. Then there will be my ACK scan, which was the ACK flag probe, then Window, then my Maimon, then UDP scan, after which there are Xmas, Null, and FIN scans. So basically, this is what we are going to do, okay? So let's see them one by one. And for now, we have to do a **TCP full scan**, right? So for that, the option I have is Nmap minus..."

**[Section 2: TCP Full Scan, SYN Scan, and ACK Scan]** "...sT. This will be a full scan, completing the three-way handshake, and output—I mean, my target is the same. Let's see what its output is. The output will be the same. I mean, this scan, the handshake, will go to every different port, and if the handshake is completed, it will tell us about this port. So there is no change in the output for this. Now, the second type is **TCP half-open scan**, which we also call SYN scan. So that is our minus s capital S (`-sS`). Then our target is the same. So this is the SYN scan, which is a half-scan. And its output will also be the same, but it's a different method. You can bypass some things with this. Like I explained in the theory, right? I am not explaining it again; you can check what was there. Its benefit is the same. Then our next one is the **ACK scan**; we will do minus s capital A (`-sA`) here. So here, what is it showing? That 'all 1000 scanned ports are unfiltered'. I had told you that when a port is unfiltered, there is no third party in between, there is no firewall, the port is not filtered. So it cannot determine whether it is open or..."

**[Section 3: UDP Scan and TCP Stealth Settings]** "...closed. Okay? So this scan is not very useful. I mean, there is no one in between. This port is not filtered, so it would be directly connected. From this, open and closed are not being determined here, though we are getting the MAC address, but okay, so it's not of much use. Then we try the next one. What was our next one? It was the **UDP scan**. So for UDP scan, the option is minus capital U (`-sU`). But in UDP, you must remember that when there is no response on any port, that port will be open. But when an 'unreachable' response comes via ICMP, then that port is closed. Okay? So it is not running right now, but for now, you keep this in mind when you run this UDP scan. So now let's try the next, and we had three more here in TCP itself; when we type help for it using minus h (`-h`) and go up, here we have `n`, `f`, and `x` for Null, FIN, and Xmas scans. So let's try all three one by one. `-s`, this is for Null, `-s` capital N (`-sN`). So in a **Null scan**, what happens is you get to know that open..."

**[Section 4: Null and FIN Scans]** "...and filtered, these two states are shown here, pay attention here: 'open|filtered'. When there is no response from an open port, it means that the packet might have been dropped in between—that can happen, right? So we cannot determine whether this port is actually open or filtered. That's why such a response comes, because that is the principle of the Null scan. Now the next one... okay, Null is done. After that is the **FIN scan**—the scan we perform by setting the FIN flag in the packets, which is showing here, again showing 'open|filtered'... showing open. So I mean, this is not of much use. But then when you use firewalls and such, we have to use this with them. This kind of output comes because what we are doing in this case is we are determining that it is open when there is no response. But when there is no response, it is possible that the packet was dropped in between, which is why there is no response. So that's why we also consider it as filtered. So there is such a principle in this, because we are not completing the handshake..."

**[Section 5: Xmas Scan and Logging Evasion]** "...we are not completing it, okay? That is why such a response comes, whether you do FIN or Null. And then the next one, let's try and see. Now the next one is when you set FIN, PUSH, and URGENT all three together, it becomes an **Xmas scan**. So let's try Xmas here: `-s` capital X (`-sX`), and my target will always remain the same. So on an Xmas scan too, a similar output will come. It shows on no response. It will show both open and filtered. So if you want to use it to avoid... to bypass firewalls or something like that, you have to try something like this. But when... I don't mean to avoid firewalls—I mean, it will bypass them, but if your target system logs things or records activity, Nmap can be detected in any full scan. It is logged less in half-open. That's why we do half-open. And then we set flags like FIN, RST, URGENT like this. So, I mean, we are doing this to avoid the logging that happens. We are doing this to remain anonymous. Firewall bypass happens in very few cases..."

**[Section 6: Conclusion]** "...I haven't explained firewall bypass to you properly yet. We will study how to bypass firewalls with Nmap in the next video. In this video, I mean, we do all this to avoid target logging and to remain anonymous. That's why we have to deal with this 'open|filtered' state in this case. So these were all the types of scans that I explained to you in theory. I hope you have understood now what kind of output comes from all of these. This is how the output is. Okay? So keep watching for the next videos, keep learning, and goodbye."