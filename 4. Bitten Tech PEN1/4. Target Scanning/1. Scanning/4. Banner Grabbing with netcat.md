
```
NAME
     nc - TCP/IP swiss army knife

SYNOPSIS
     nc [-options] hostname port[s] [ports] ...
     nc -l -p port [-options] [hostname] [port]

DESCRIPTION
     netcat  is  a  simple Unix utility which reads and writes data across network connections,
     using TCP or UDP protocol. It is designed to be a reliable "back-end"  tool  that  can  be
     used  directly  or easily driven by other programs and scripts.  At the same time, it is a
     feature-rich network debugging and exploration tool, since it can create almost  any  kind
     of  connection  you would need and has several interesting built-in capabilities.  Netcat,
     or "nc" as the actual program is named, should have been supplied long ago as another  one
     of those cryptic but standard Unix tools.

     In  the  simplest  usage, "nc host port" creates a TCP connection to the given port on the
     given target host.  Your standard input is then sent to the host, and anything that  comes
     back  across the connection is sent to your standard output.  This continues indefinitely,
     until the network side of the connection shuts down.  Note that this behavior is different
     from most other applications which shut everything down and exit after an  end-of-file  on
     the standard input.
     Netcat  can  also  function as a server, by listening for inbound connections on arbitrary
     ports and then doing the same reading and writing.  With minor limitations, netcat doesn't
     really care if it runs in "client" or "server" mode -- it  still  shovels  data  back  and
     forth until there isn't any more left. In either mode, shutdown can be forced after a con‐
     figurable time of inactivity on the network side.

     And  it  can  do this via UDP too, so netcat is possibly the "udp telnet-like" application
     you always wanted for testing your UDP-mode servers.  UDP, as the "U" implies, gives  less
     reliable  data transmission than TCP connections and some systems may have trouble sending
     large amounts of data that way, but it's still a useful capability to have.

     You may be asking "why not just use telnet to connect to arbitrary ports?" Valid question,
     and here are some reasons.  Telnet has the "standard input EOF" problem, so one  must  in‐
     troduce  calculated  delays in driving scripts to allow network output to finish.  This is
     the main reason netcat stays running until the *network* side closes.   Telnet  also  will
     not  transfer  arbitrary binary data, because certain characters are interpreted as telnet
     options and are thus removed from the data stream.  Telnet also emits some of its diagnos‐
     tic messages to standard output, where netcat keeps such things religiously separated from
     its *output* and will never modify any of the real data in  transit  unless  you  *really*
     want  it  to.   And of course telnet is incapable of listening for inbound connections, or
     using UDP instead.  Netcat doesn't have any of these  limitations,  is  much  smaller  and
     faster than telnet, and has many other advantages.


OPTIONS
     -c string    specify  shell commands to exec after connect (use with caution).  The string
                  is passed to /bin/sh -c for execution.  See the -e option if you don't have a
                  working /bin/sh (Note that POSIX-conformant system must have one).

     -e filename  specify filename to exec after connect (use with caution).  See the -c option
                  for enhanced functionality.

     -g gateway   source-routing hop point[s], up to 8

     -G num       source-routing pointer: 4, 8, 12, ...

     -h           display help

     -i secs      delay interval for lines sent, ports scanned

     -l           listen mode, for inbound connects

     -n           numeric-only IP addresses, no DNS

     -o file      hex dump of traffic

     -p port      local port number (port numbers can be individual or  ranges:  lo-hi  [inclu‐
                  sive])


     -q seconds   after  EOF  on  stdin, wait the specified number of seconds and then quit. If
                  seconds is negative, wait forever.

     -b           allow UDP broadcasts

     -r           randomize local and remote ports

     -s addr      local source address

     -t           enable telnet negotiation

     -u           UDP mode

     -v           verbose [use twice to be more verbose]

     -w secs      timeout for connects and final net reads

     -C           Send CRLF as line-ending

     -z           zero-I/O mode [used for scanning]

     -T type      set TOS flag (type may be  one  of  "Minimize-Delay",  "Maximize-Throughput",
                  "Maximize-Reliability", or "Minimize-Cost".)


```

```
└─# nc -h     
[v1.10-50.1]
connect to somewhere:   nc [-options] hostname port[s] [ports] ... 
listen for inbound:     nc -l -p port [-options] [hostname] [port]
options:
        -c shell commands       as `-e'; use /bin/sh to exec [dangerous!!]
        -e filename             program to exec after connect [dangerous!!]
        -b                      allow broadcasts
        -g gateway              source-routing hop point[s], up to 8
        -G num                  source-routing pointer: 4, 8, 12, ...
        -h                      this cruft
        -i secs                 delay interval for lines sent, ports scanned
        -k                      set keepalive option on socket
        -l                      listen mode, for inbound connects
        -n                      numeric-only IP addresses, no DNS
        -o file                 hex dump of traffic
        -p port                 local port number
        -r                      randomize local and remote ports
        -q secs                 quit after EOF on stdin and delay of secs
        -s addr                 local source address
        -T tos                  set Type Of Service
        -t                      answer TELNET negotiation
        -u                      UDP mode
        -v                      verbose [use twice to be more verbose]
        -w secs                 timeout for connects and final net reads
        -C                      Send CRLF as line-ending
        -z                      zero-I/O mode [used for scanning]
port numbers can be individual or ranges: lo-hi [inclusive];
hyphens in port names must be backslash escaped (e.g. 'ftp\-data').

```

```
└─# nc -nv 192.168.60.138 80
(UNKNOWN) [192.168.60.138] 80 (http) open
HTTP/1.1 200
<!DOCTYPE HTML PUBLIC "-//IETF//DTD HTML 2.0//EN">
<html><head>
<title>400 Bad Request</title>
</head><body>
<h1>Bad Request</h1>
<p>Your browser sent a request that this server could not understand.<br />
</p>
<hr>
<address>Apache/2.2.8 (Ubuntu) DAV/2 Server at metasploitable.localdomain Port 80</address>
</body></html>

```


```
└─# nc -nv 192.168.60.138 22
(UNKNOWN) [192.168.60.138] 22 (ssh) open
SSH-2.0-OpenSSH_4.7p1 Debian-8ubuntu1

```

```
└─# nc -nv 192.168.60.138 21
(UNKNOWN) [192.168.60.138] 21 (ftp) open
220 (vsFTPd 2.3.4)

```

**[Part 1: Introduction & Netcat Command Line]**

"So, friends, in this video we will talk about another tool with which we can grab banners. Okay? So here, let's open our command prompt—I mean, the terminal, sorry, I am currently in Linux so I will call it terminal. Okay? Now what do we have to do here? We have to type a command. We are going to use a tool called **Netcat**. So Netcat is used for banner grabbing, and it has many other uses too, not just simple banner grabbing. Okay? So Netcat's command is simple: **'nc'**. This is 'nc'. So when you click on it—I mean, when you type 'nc', the nc command line opens up. After that, you will have to write the nc commands. To quit, I will simply press **Control + C** here. Okay? Now we need to find out about the nc manual. Let's first check the manual to see what it actually is. So here you can see..."

**[Part 2: TCP/IP Swiss Army Knife Overview]**

"...**TCP/IP Swiss Army Knife**—meaning you can do all TCP/IP work with this. It is a simple Unix utility which reads and writes data across network connections using the TCP/IP protocol. This means that by using the TCP protocol, you can send any data you want to any remote device, or you can also use UDP. It is basically used for debugging and exploration by network administrators, but penetration testers use it a lot to establish any type of connection with a remote host because it can establish any kind of connection, as stated here. Okay? So we will try using it. In its simplest usage, you have to provide **'nc [host] [port]'**. This means establishing a TCP connection with that remote system. By simply writing 'nc', then the host name, and the port you want to connect to, it will connect you in a simple way. Okay? And then, whatever response comes, it will show the response here—whatever the remote host responded with. And you will keep getting all the output here, whatever the remote host is sending to you on the command line until that connection is closed."

**[Part 3: Netcat Persistence and Listener/Server Mode]**

"...you will keep getting all the output. Whatever the remote host is sending to you on the command line, until that connection is closed, you will keep receiving everything. So how is it different? It has mentioned here that most other applications shut down and exit when there is an End-Of-File (EOF) on the standard input. This means that after getting a single output on the standard input, they finish—meaning most command outputs terminate after a single output. But this is not like that; as long as the connection is not closed, it will keep running. You can read more about it by going down; it has a very large description. And not just as a client, you can also use Netcat to **turn your system into a server**. For that, there is an option to **listen**. You have to listen on a particular port, and then whoever sends a connection on that port, you have to accept it, and Netcat will accept it for you. So a simple server is easily set up here. Here are its options..."

**[Part 4: Banner Grabbing Strategy and IP Configuration]**

"...We will look at all these options gradually as we move forward. But for banner grabbing here, we don't need to use any special options. I will tell you about the different options later. What we need to do here is apply a trick to grab the banner. For that, we need to come down here. First of all, let's quit this. So on this new screen, I will type a command, and you should type it exactly the same way. If we want to connect to a server or a device, the simple command is 'nc'. After that, the host name—either you give the host name or you give your IP address, which is your remote target's IP. Here, I am giving the IP address of my **Metasploitable machine** because I want to grab its banner. Okay? So I am giving the IP of my Metasploitable machine. If you are providing an IP, you will have to type an option here: **'-n'**. This means that you are providing a numerical address, meaning you are not giving a host name. Okay? Okay? So my Metasploitable machine's..."

**[Part 5: Connection and Verbosity Controls]**

"...IP is this. I'll type it, and after that, the port number—which port do you want to connect to? For now, I would like to connect to the HTTP port. So the port number is **80**. Okay? Simple. And we want **verbose output** here—meaning exactly what is happening in the background, I want that output. It should show whatever is happening. So for that, I will write **'-v'** here. '-v' stands for verbose, and if you want to be more verbose, you write **'-vv'** twice. But you also know another trick: since you are writing '-n' here, you can just put 'v' inside it as well [e.g., `-nv`], and both things will be done. This means there is no need to write minus-minus separately again and again. So this is our command. After typing this command, it will find out if a TCP connection is being established or not. So first of all, it will establish a TCP connection on this port. Let's see if it works. Okay. So here it is showing **'80 http open'**. This means we can establish a connection on port 80. Now, our job is not finished here; we have only found out that we have established a connection..."

**[Part 6: Triggering HTTP Bad Request to Grab Banner]**

"...but now we have to send some data so that we can grab the banner. Because we have to send an **HTTP request**, right? We haven't sent that yet; we have only established a connection so far. So, to send a request, we will have to **generate an HTTP error**. A request that the other side cannot understand, and in return, we get its address—meaning we get its banner. So what will we do for that? We will write a command here. You write it exactly as I say. You can make minor changes, but there shouldn't be too many changes. Write it just as I am writing: **'http 1.1'** or **'1.0'**, whichever you write is up to you. After that will come **'200'** here. You will write '200' and then simply press enter. So here some response came. As you can see, **'400 Bad Request'**. It did not understand this request. So it says 'Bad Request' and says 'Your browser sent a request that this server could not understand.' So it didn't understand. But our job is done! Why? How? Look here. This is the address tag, and inside it, you can see our banner..."

**[Part 7: Analyzing Apache Banner and Running Port 22 SSH Grab]**

"...**'Apache 2.2.8'**. This is our web technology which is being used in the backend. This is the server. This server is being used in the backend, and its version is this. And its host name is **'metasploitable.localdomain'**—it told us this too. It is using a **DAV/2 server**, this type, okay. **Ubuntu**—see, we got the OS too! We got the OS, and OS fingerprinting is done as well. Now, in Metasploitable there is some configuration because of which we are getting everything, but still, we get a lot of information. So this is how you can grab a banner. Here I have done it on Metasploitable, but you can do it on any remote server. Whether you type a domain name or write an IP, it doesn't make a difference, just provide the option for it. So this is how banner grabbing is done on port number 80. If you want, you can grab banners for **SSH, FTP, for everything**. For example, let's say I want the banner for **port number 22**. Meaning, which version of SSH is it, which type is it? Which one is it using? To find that out, you can see here, simply by typing '22' we get..."

**[Part 8: Running Port 21 FTP Grab, Summary and Closing]**

"...it: **'SSH-2.0-OpenSSH_4.7p1 Debian-8ubuntu1'**. So this is the SSH banner. And then, if we want FTP, **port number 21**. This FTP is using **'vsFTPd 2.3.4'** version. Now, which of these versions are vulnerable, and for which are exploits available? That is for you to define, and we will see later how we do that. But simply, this is how banners are grabbed. **Netcat is a very powerful tool**, as you can see—you can grab banners on every port. If you did it through a browser, you would only get the HTTP banner, but here there is so much more. So this is how it is done, and this is how you can do it. That's all for this video. Keep watching for the next videos, keep learning, and goodbye."