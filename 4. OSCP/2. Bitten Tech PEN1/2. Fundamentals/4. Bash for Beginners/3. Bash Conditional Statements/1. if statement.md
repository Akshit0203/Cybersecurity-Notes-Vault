
```
#!/bin/bash

echo "Enter your password:"
read pass

if [ expression/condition ]; then

	commands

fi

```

```
#!/bin/bash

echo "Enter your password:"
read pass

if [ $pass == "secret" ]
then

	echo "Login successful"

fi

```

```
└─$ ./new.sh
Enter your password:
secret
Login successful

```

```
#!/bin/bash

echo "Enter your password:"
read pass

if [ $pass == "secret" ]
then

	echo "Login successful"

else 

	echo "Login not successful"

fi

```

```
└─$ ./new.sh
Enter your password:
test
Login not successful

```

```
#!/bin/bash

echo "Enter your number:"
read int

if [ $int -lt 100 ]
then

	echo "$int is less than 100"

else 

	echo "$int is greater than 100"

fi

```

```
└─$ ./new.sh
Enter your number:
12
12 is less than 100

```

```
#!/bin/bash

echo "Enter your number:"
read int

if [ $int -gt 100 ]
then

	echo "$int is greater than 100"

else 

	echo "$int is less than or equal to 100"

fi

```


```
└─$ ./new.sh
Enter your number:
101
101 is greater than 100

```

```
#!/bin/bash

echo "Enter your number:"
read int

if [ $int -ge 100 ]
then

	echo "$int is greater than or equal to 100"

else 

	echo "$int is less than 100"

fi

```

```
Enter your number:
100    
100 is greater than or equal to 100

```

```
#!/bin/bash

echo "Enter your number:"
read int

if [ $int -ge 100 ]
then

	echo "$int is greater than or equal to 100"

else 

	echo "$int is less than 100"

fi

```


```
#!/bin/bash

echo "Enter your number:"
read int

if [ $int -le 100 ] && [ $int -gt 90 ]
then

	echo "Grade A"

else 

	echo "Grade not A"

fi

```


```
└─$ ./new.sh
Enter your number:
89
Grade not A
                                                                                                
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ./new.sh
Enter your number:
91
Grade A
                                                                                                
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ./new.sh
Enter your number:
101
Grade not A

```


```
#!/bin/bash

echo "Enter your number:"
read int

if [[ $int -le 100 && $int -gt 90 ]]
then

	echo "Grade A"

else 

	echo "Grade not A"

fi

```

```
Enter your number:
95
Grade A
                                                                                                
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ./new.sh
Enter your number:
90
Grade not A
```

```
#!/bin/bash

echo "Enter your number:"
read int

if test $int -lt 100
then

	echo "Grade A"

else 

	echo "Grade not A"

fi

```

```
Enter your number:
89
Grade A
```


```
#!/bin/bash

echo "Enter your number:"
read int

if [ $int -le 100 ] && [ $int -gt 90 ] 
then
        
	echo "Grade A"

elif [ $int -le 90 ] && [ $int -gt 80 ]
then 

	echo "Grade B" 

elif [ $int -le 80 ] && [ $int -gt 70 ]
then 

	echo "Grade C" 

else 

	echo "Grade not A"

fi

```

```
Enter your number:
75
Grade C
                                                                                                
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ./new.sh
Enter your number:
85
Grade B

```

```
#!/bin/bash

echo "Enter first number:"
read a
echo "Enter second number:"
read b
echo "Enter third number:"
read c

if [ $a -gt $b ] 
then 

	if [ $a -gt $c ]
	then 

		echo "$a is greatest"

	else 

		echo "$c is greatest"
	fi

else 

	if [ $b -gt $c ]
	then 

		echo "$b is greatest"

	else 

		echo "$c is greatest"
	fi

fi
```

```
Enter first number:
5
Enter second number:
10
Enter third number:
15
15 is greatest
```

```
#!/bin/bash

echo "Enter first number :"
read num

if [[ $num -gt 10 && $num -lt 100 || $num -lt 10 && num -gt 0 ]]
then 
	echo "True"
fi
```

```
Enter first number :
9
True
                                                                                                
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ./new.sh
Enter first number :
10
                                                                                                
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ./new.sh
Enter first number :
11
True
                                                                                                
┌──(kali㉿kali)-[~/bash-scripts]
└─$ ./new.sh
Enter first number :
101

```



**[Part 1: Introduction to Bash Conditional Statements & Syntax]**

Hello friends. From this video, we are starting conditional statements in Bash. So I have created a separate file here, and I would also suggest that you do different topics in separate files, okay? Here we will check some condition where we need to execute a logic only when a particular condition is true. For instance, a variable should have some value, or we want to compare less than or greater than conditions. We need to do all these things so that the logic is not computed every time, but rather executes based on a certain condition every time.

For that, I have taken a use case: "Enter your password". I am asking the user for a password and storing it in `pass`. And I will check in a hardcoded way in the application, just for example, whether that password is equal to a specific string. If it is equal to that, then your password is correct, login successful; otherwise login is false. So I will write `if`. I am giving you a general idea right now, a syntax for how `if` is written in Bash, okay? So `if` is written like this here: `if condition`.

Just like you do in normal programming. After that, you have to give square brackets. These square brackets can be single, or they can be double. Right now, I am talking about single brackets. You have to leave spaces between both of them, okay? Again, here you have to give your expression—the condition you want to specify that will evaluate to true or false. You can call it an expression or condition that returns true or false to you. According to that, you then execute your condition here. After that, a semicolon comes here. Let me tell you that the semicolon is not strictly necessary. A semicolon is necessary when you write `then` on the same line. And this `then` is a part of the syntax: `then` execute, sorry, commands. Inside this, these commands will be executed. How will it end? With `fi`.

So you can say that this is the block of `if`. If this condition is correct, this expression evaluates to something and returns true or false. If it gives true, then it will execute. On false, it will move forward, coming directly down below. If it turns out true, then whatever is inside these commands that I have written—whatever you write in this—will get executed normally, how it works. Writing `then` is necessary because `then` is part of the syntax, and this semicolon will come when you place `then` on the same line. If you don't place `then` on the same line, but write it on the next line like this, then there is no need for a semicolon here; it will work without a semicolon.

So this is our general syntax for the `if` condition. What will we do in this? The condition—you have to replace this. So what is my condition? The password should match a particular string. To match it, what do I do? I apply a comparison operator. Normally, I will access the value of `pass` here and check with the equality operator. Then giving a space, there is my string which is hardcoded "secret". So if the password is "secret", then I need to run the command; then my condition is correct. So then what will I do? I will say "login successful". Meaning if the password is "secret", then your login is successful. So this is our simple `if` statement.

---

**[Part 2: Demonstrating String Comparison &** **if-else** **Logic]**

Now let's see how it works. I will enter something in the command line. Make sure that your script is executable. Mine already is, so I execute it and it is asking for a password. Now I will enter "secret", S-E-C-R-E-T: "login successful". Look, it got executed! Meaning here it is checking that it is "secret" and doing a string comparison. It is successful. And after this, if I enter something weird like ABCD, then it said nothing. Meaning your login is not successful; it won't do anything. This is a simple use case when you enter the password.

And here in the `if` statement, you will know that it is `if-else` in a way. If `if` is not correct—meaning if `if` is not returning true—then the `else` condition always executes. So `if` and `else` are mutually exclusive, okay? Now here, where you wrote `fi`, your `else` will come inside before this `fi`. Whatever command you want to take after `then`, you keep inside this indentation. Giving indentation inside `then` is necessary. After that, `else` will come here before `fi`, because the entire block will end at `fi`.

And what to write in `else`? Simply `echo "login not successful"`, okay? This is simple, and I run it again. If I enter "secret", it gives correct; if I enter something else, then "login not successful". Simple, this is no big deal. And I did the same, so keep in mind that `fi` is the reverse of `if`—just keep that, and your job is done.

---

**[Part 3: Numeric Comparisons & Operators (****-lt****,** **-gt****,** **-ge****,** **-le****)]**

Now, this was working with strings. If you want to work with numbers, I will write here "enter your number" and enter an integer, `read int`, and I will check whether its value is equal to a particular value. Is it less than? Is it greater than? You can do it in all these ways. So here there will be a dollar, `$int`. If you want to check whether this number is less than 100, I will apply a switch-like operator: `-lt` for less than, okay? There are many like this. `-lt` means less than, and then I will write the number 100, okay?

So it will be like this: `if [ $int -lt 100 ]`, then I will say that the number—I will print that number itself: `$int is less than 100`. And if not, `$int is greater than 100`, simple. So how does it work here? I enter the number 100, so it says "100 is greater than 100". Meaning, what is less than? It doesn't check equal; less means it must strictly be less. So for that I will enter 99, so "99 is less than 100", so this got executed. This is less than.

Now similarly, there is greater than (`-gt`) as well. If you want to check whether it is greater than, then here will come greater, and then here will come less than and equal to—my mistake here—so when I am checking greater than, its exclusion (meaning whatever else remains) can also be equal, right? And "equal to and" won't come here, sorry, "or" will come, "or equal to". So is it greater than 100? Let's check 111, so it is greater.

Now you also have cases where you need to check less than or equal to (`-le`), or greater than or equal to (`-ge`). So for that, will you apply this operator two or three times? There is no need for that. For that, they have given `-ge` here for greater than or equal to. So it will check both: if it is equal to, it is true; if it is greater, it is also true—greater than or equal to, and less than will come here in `else`. So let's check, I will try entering 100 to see if it works or not: 100, okay, so look here: greater than or equal to 100. So similarly, there is less than (`-le`) here as well. So you can use it like that; I won't explain it repeatedly, you can use it like this.

---

**[Part 4: Logical Operators & Single vs. Double Square Brackets (****[ ]** **vs** **[[ ]]****)]**

Now these were our simple comparison operators. Besides these, there are other things as well, like if you want to check two conditions together. As you have studied logical operators, right? OR, AND, and NOT. All those operators will be useful to you here as well. So for that, what do you need to do? You have a single square bracket option. Outside this, you can place a logical operator. So you can check two expressions, two conditions together at the same time. Like here is one: if `$int` is less than or equal to 100, then I need to do grading here. For example, if your marks are between 90 and 100, then your grade will be A.

So that is what I am going to check: if it is less than or equal to 100 AND—if I want to check AND, then here is the AND operator, double ampersand (`&&`), then after that I have to give square brackets again, and apply it the same way with spaces: `$int` greater than 90. So it should be above 90 and equal to or less than 100, greater than 90, then my grade will be A, Grade A. And besides that, I write "Grade Not A" here for now. Here I placed two conditions and AND, so what happens in AND? Both conditions must be true. So only when both are fulfilled will it give Grade A. So let's take this: right now I enter the number 90, so see: "Grade Not A". It won't work for 90. If I give 95, then see: "Grade A". And if I give 100 here, then "Grade A" will come too. If I give 11, it will say "Not A" because that is invalid. You can check that condition as well.

But look here, you have to place square brackets twice here. To avoid that, what can you do? You can place double square brackets `[[ ]]`. Double means you don't have to give separate brackets; you won't need to open and close them separately. Now right here, place double square brackets and close them twice here too. And in between, whatever sign you want to place—AND or OR—you can apply that. So there is no need to open and close separately here; inside double square brackets, the entire expression will fit within one block. So it will work the same way, no difference. I gave 90, it gave "Grade Not A".

---

**[Part 5: POSIX Standards, the** **test** **Command, & Portability]**

But I will tell you one problem in this later on. Here in `if`, there are other ways to give expressions. Like let me tell you, as I gave square brackets, if you follow the POSIX standard—if you don't know POSIX standards, I will tell you later on—there is a `test` command/operator that is used with `if` conditions to test any condition. Inside `test`, if you run `man test` in the shell, it will give you all the documentation. So what is `test`? It is simply similar to your square brackets. So you can replace square brackets with `test`. This was created earlier, I think. So what I wrote there, `$int` less than 100, so it will work here as well. Simple, no change in this. Right now I gave 95: Grade A. So right now it is less than, so it gives Grade A, and if it becomes greater, 101, then it gives "Not A".

This `test`, you see, is a replacement for single square brackets. Meaning it works just like that: use square brackets or use `test`, there is no difference. But if you use the double square bracket `[[ ]]` in this manner, then from the outside, it won't seem like you did anything wrong. But the problem arises when you work on portability. Meaning, you must have heard about POSIX, whose full form is Portable Operating System Interface. It has specified standards, just like IEEE has specified networking standards for what all we can use today. Similarly in Unix, whatever programming you do, standards are defined for what symbols you can use for portability.

If you want to run this program on another platform, like BSD or other operating systems, because if you work in Unix and build a script on one system and execute it on another system, then to remove those compatibility issues, standards were created. So everyone will use `test` or single square brackets, so when you change platforms there won't be an issue; you will be able to execute on every platform. But if you use this double square bracket `[[ ]]`, it is not a POSIX standard. It was made more for convenience. But if you use this, you won't face problems unless you work as a sysadmin across many legacy systems, in which case you might face issues. But today, I don't think it should be a problem. If you read deeply about it, you can read, but this can also be used. Okay, I am not forbidding it, just use it carefully because it is not standard. Following standards is always better, so I would suggest following standards and using single brackets or `test`.

---

**[Part 6:** **elif** **Ladders & Nested** **if** **Statements]**

Now in `if-else`, as you might have studied, there is also `elif`. Meaning if one `if` condition isn't met, then another one can be. Then another one can be. In this way, there can be a number of `elif` statements together. Like here, what did I check? That it should be less than or equal to 100 AND `$int` > 90, so it was Grade A. Now after that, I will place `elif`. Right below `if` and above `else`, I will place `elif`. Just like you do in Python, here too. I will copy this entire thing and just change the numbers here: less than or equal to 90 AND greater than 80. So what will come in that? I copy this `echo` again, and inside `elif` I also need to give `then`: `then echo "Grade B"`. So one `elif` came. If this grade didn't happen, then it will be Grade B. If this doesn't happen, then `else` will definitely run.

So now let's see: if I enter marks as 85, I get Grade B. And if it is 95, then Grade A will come. So it works like this, and you can add more `elif` statements. You can use as many as you can think of. Here 80 will come, then 70 here, and then this becomes Grade C. So if I enter 75 here, it gives me Grade C. So simply keep adding `elif` ladder like this; this is called an `elif` ladder. And the last condition will be default (`else`), which will always execute finally. So this is one way.

And another thing is, here you can also use nested `if` statements. In nested, what happens? For example, if I need to compare three numbers. If I input two numbers, that becomes simple `if-else` to see which is greater. But if I need to compare three numbers, first I compare two, then whichever turns out larger, compare it with the third, and then we find out which is the largest number. So if we want to implement that, let's input three numbers here. So see, I have taken three number inputs: first, second, and third into `a`, `b`, and `c`.

And first I will check in the first condition whether number `a` is greater than `b`. If that happens, then inside this `then` block, I will place another `if` statement inside it. Now that I know `a` is larger between `a` and `b`, I will check `a` against `c`. If `a > c`, then I know the largest number is `a`, so I will `echo "a is greatest"`. Okay, now similarly if I go to `else`, if `a` is greater than `b` and `a` is smaller than `c`, then obviously `c` is the largest, so "c is greatest". And here in the `else` part, the same two things will come, but the difference here will be that `b` is greater than `a`, so the comparison will be between `b` and `c`. If `b > c`, then "b is greatest", otherwise "c is greatest". So this is nested `if`.

Now let's run this and see if it runs correctly. Enter first number: 5, 10, 15. Let's see which is the largest. Okay, so here there is a slight syntax error. After `else`, okay, the problem is that you haven't closed the inner `if` block with `fi`. So let me close it. These problems will happen. It's good that this error occurred, because you will know what mistake you made. So yes, you must close the `if` block with `fi`. So 5, 10, 15: 15 is greatest. It went into this. After that, if I enter 100, or 300, and then 200, which is the largest? 300. So it is giving the correct answer, meaning my logic is working properly. In this way, you can place more `if` statements inside nested blocks. Here, you could also have done it using AND, but I wanted to demonstrate nested `if`, so that's why this worked here. And in this way, you can also use NOT and all that. I am not showing all of that, I will give you references to practice from. And I will also give you these files that I created in front of you.

---

**[Part 7: Complex Multi-Condition Expressions & Conclusion]**

And there is also a case where you have to give multiple logical statements together, as I have given here. So I used the double bracket syntax `[[ ]]`. I took a number input and checked it. Here is a block: I checked if a number is greater than 10 AND the number should be less than 100. It is between 10 and 100, so it will return true. Or—look here, there is OR (`||`) in between—then the second condition is that it should be less than 10 AND greater than zero. So if it is between 0 and 10 or between 10 and 100—if either of these conditions (this one OR that one) turns out true, then the inner statement "true" will be printed.

So now I show you how this is working. I will run this and enter a number, for example 10, so it didn't print "true". Meaning 10 won't work either because you gave strictly greater than and less than. If you want to check 10 as well, you need to provide equal to. And for equal, if you want to check equal only, there is `-eq`. Okay? `-le` and `-ge` I told you earlier, so `-eq` is also here, keep that in mind. This is greater than 10, and it doesn't work on 10. If I give 9, then it gives true, because what was the condition here? Look, the second one: less than 10 AND should be greater than zero. So 9 is greater than zero. After that, if I give 85, it gives true, because in the first condition 85 fits in between. If I enter above 100, it won't give true because there is no condition for that.

So whatever combinations of logical operators, square brackets, greater than, etc., you want to apply, it is up to your imagination and creativity—how much you can apply. Keep practicing, and you can build some real-world type projects with this. So when future concepts come after finishing the entire course, move to those, but right now just build basic examples. Convert programming tasks into Bash, you will enjoy it a lot. So I think for `if` conditions and `if` statements, that's enough for today. I have explained almost everything to you, and you got to know the basic usage. That's all for this video, we will move forward in the next video. So keep watching, keep learning, and goodbye!

