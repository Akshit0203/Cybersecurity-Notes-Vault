Boot the Windows Server 2022 (Domain Controller) VM. Server Manager opens automatically.

## Organize Active Directory Users & Computers

Go to **Tools** → **Active Directory Users and Computers**.

![](attachments/image-210.png)

Inside the **Users** container you'll see built-in groups mixed in with user accounts. By default, the only real login account is **Administrator** (the Guest account is disabled — note the down-arrow icon).

![](attachments/image-211.png)

To keep things tidy, create a new **Organizational Unit (OU)** called `Groups`: right-click → **New** → **Organizational Unit**. Then select all the built-in group objects and **move** them into this new OU.

![](attachments/image-212.png)

![](attachments/image-213.png)

![](attachments/image-214.png)

![](attachments/image-215.png)

![](attachments/image-216.png)

![](attachments/image-217.png)

Now the Users container should only have the **Administrator** account and the disabled **Guest** account.

---

## Create User Accounts

### Domain Admin — Tony Stark

Right-click the **Administrator** account → **Copy**. This copies all its permissions (Domain Admins, Enterprise Admins, etc.).

![](attachments/image-218.png)

You can double-click Administrator to inspect its properties. Under the **Member Of** tab you'll see all its group memberships — Domain Admins, Enterprise Admins, etc. The copy operation duplicates this.

![](attachments/image-219.png)

![](attachments/image-220.png)

![](attachments/image-221.png)

Create the account: **Tony Stark**, logon name `tstark` (first initial + last name convention). Set a password and check **Password never expires**.

![](attachments/image-222.png)

![](attachments/image-223.png)

![](attachments/image-224.png)

![](attachments/image-225.png)

### Service Account — SQLService

Copy Administrator again. This time create a **service account** named `SQLService`. Service accounts are used to run services (e.g., SQL Server). We won't actually install SQL Server — this is for demonstrating an attack later.

![](attachments/image-226.png)

![](attachments/image-227.png)

Set a password. Check **Password never expires**.

![](attachments/image-228.png)

![](attachments/image-229.png)

![](attachments/image-230.png)

> [!IMPORTANT]
> Open the SQLService account's **Properties** and put the password in the **Description** field. This simulates a very common real-world misconfiguration — admins often store passwords in description fields thinking only they can see them. **Any valid domain user can read account descriptions.**

![](attachments/image-231.png)

### Standard Users — Frank Castle & Peter Parker

These are low-privilege accounts (Domain Users only). Right-click on white space → **New** → **User**.

![](attachments/image-232.png)

Create **Frank Castle** with logon name `fcastle`. Check **Password never expires**.

![](attachments/image-233.png)

![](attachments/image-234.png)

![](attachments/image-235.png)

![](attachments/image-236.png)

Copy Frank Castle to create **Peter Parker** with logon name `pparker`. Same settings.

![](attachments/image-237.png)

![](attachments/image-238.png)

![](attachments/image-239.png)

![](attachments/image-240.png)

### Summary of Accounts

| Account       | Logon Name   | Type / Group        | Notes                                |
| ------------- | ------------ | ------------------- | ------------------------------------ |
| Administrator | administrator | Domain Admin        | Built-in                             |
| Tony Stark    | tstark       | Domain Admin        | Copied from Administrator            |
| SQLService    | SQLService   | Domain Admin (service) | Password stored in description field |
| Frank Castle  | fcastle      | Domain User         | Standard user                        |
| Peter Parker  | pparker      | Domain User         | Standard user (copied from fcastle)  |

![](attachments/image-241.png)

---

## Create an SMB File Share

In **Server Manager** → **File and Storage Services** → **Shares**.

![](attachments/image-242.png)

Click **Tasks** → **New Share**.

![](attachments/image-243.png)

Select **SMB Share – Quick**. Hit **Next**.

![](attachments/image-244.png)

Leave the share location on the **C: drive**.

![](attachments/image-245.png)

Name the share `hackme`. This creates:
- Local path: `C:\Shares\hackme`
- Remote path: `\\WINDOWS-DC\hackme`

![](attachments/image-246.png)

Allow caching of the share. Leave permissions at defaults. Hit **Create**.

![](attachments/image-247.png)

![](attachments/image-248.png)

![](attachments/image-249.png)

![](attachments/image-250.png)

---

## Register the SPN for SQLService

Open **Command Prompt as Administrator**.

![](attachments/image-251.png)

Register a Service Principal Name (SPN) for the SQL service account:

```cmd
setspn -a Windows-DC/SQLService.DC.local:60111 DC\SQLService
```

![](attachments/image-252.png)

### What is an SPN?

An SPN (Service Principal Name) is a unique identifier that ties a **service endpoint** to an **AD account**. Creating the `SQLService` account alone only tells AD the account exists — it doesn't tell AD what service it runs, on which server, or on which port. The SPN creates that mapping.

### Command Breakdown

| Part | Meaning |
| ---- | ------- |
| `setspn` | Windows tool for managing SPNs |
| `-a` | **Add** a new SPN |
| `Windows-DC/SQLService.DC.local:60111` | The service name: server `Windows-DC`, service `SQLService.DC.local`, port `60111` |
| `DC\SQLService` | The AD account that **owns** this service |

### Why Kerberos Needs SPNs

When a user requests a service ticket:

> "Give me a ticket for `Windows-DC/SQLService.DC.local:60111`"

The Domain Controller looks up which account owns that SPN, finds `DC\SQLService`, and issues the ticket encrypted with that account's password hash. **Without an SPN, no ticket can be issued** — this is also why SPNs are critical for Kerberoasting attacks later.

**Before the command:** AD knows `SQLService` exists, but not what service it represents.
**After the command:** AD maps `Windows-DC/SQLService.DC.local:60111` → `DC\SQLService`.

### Verify SPNs

Query all SPNs in the domain:

```cmd
setspn -T DC.local -Q */*
```

| Flag | Meaning |
| ---- | ------- |
| `-T DC.local` | Search in domain `DC.local` |
| `-Q` | Query/search |
| `*/*` | Match all SPNs (wildcard) |

![](attachments/image-253.png)

Confirm the output includes:

```
CN=SQL Service,CN=Users,DC=DC,DC=local        Windows-DC/SQLService.DC.local:60111
```

✅ Domain = `DC.local`
✅ SQLService SPN is registered

---

## Create a Group Policy — Disable Windows Defender

Open the Start menu → search for **Group Policy Management**.

![](attachments/image-254.png)

### What is a GPO?

A **Group Policy Object (GPO)** is a set of Windows rules/settings applied to computers or users. Examples: force password complexity, disable Control Panel, run a startup script, map a network drive, install software, set wallpaper.

A GPO has two steps:
1. **Create it** — the policy exists in AD but affects nobody yet
2. **Link it** — attach the policy to a domain, OU, or site so it applies to the objects within

### Create the GPO

Expand the forest → **DC.local**. Right-click **DC.local** → **Create a GPO in this domain, and Link it here**. Name it `Disable Windows Defender`.

![](attachments/image-255.png)

![](attachments/image-256.png)

![](attachments/image-257.png)

### Edit the GPO

Right-click the new GPO → **Edit**.

![](attachments/image-258.png)

Navigate to: **Computer Configuration** → **Policies** → **Administrative Templates** → **Windows Components** → **Microsoft Defender Antivirus**.

![](attachments/image-259.png)

> [!NOTE]
> In Server 2019 and earlier, this is called **Windows Defender Antivirus** instead of Microsoft Defender Antivirus.

![](attachments/image-260.png)

Double-click **Turn off Microsoft Defender Antivirus** → set to **Enabled** → **Apply** → **OK**.

![](attachments/image-261.png)

Close the editor. Right-click the GPO → **Enforced** to ensure the policy is enforced domain-wide. Any computer that joins the domain (or receives a policy refresh) will have Defender disabled.

![](attachments/image-262.png)

---

## Set a Static IP on the Domain Controller

Open **Command Prompt** and run `ipconfig` to note the current IP.

![](attachments/image-263.png)

![](attachments/image-264.png)

Go to **Network & Internet Settings**.

![](attachments/image-265.png)

Click **Change adapter options**.

![](attachments/image-266.png)

Open the adapter's **Properties** → **Internet Protocol Version 4 (TCP/IPv4)**.

![](attachments/image-267.png)

![](attachments/image-268.png)

![](attachments/image-269.png)

Set a static IP matching your current address. Example:
- **IP Address:** `192.168.138.136`
- **Subnet Mask:** `255.255.255.0`
- **Default Gateway:** `192.168.138.2`

Hit **OK**.

![](attachments/image-270.png)

![](attachments/image-271.png)

![](attachments/image-272.png)

> [!NOTE]
> You will lose internet connectivity on this machine because the static IP has no real gateway with internet access. This is fine — we're building a local lab for attack simulation and don't need internet on the DC.

