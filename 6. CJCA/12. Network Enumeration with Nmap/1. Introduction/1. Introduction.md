
# Enumeration

---

`Enumeration` is the most critical part of all. The art, the difficulty, and the goal are not to gain access to our target computer. Instead, it is identifying all of the ways we could attack a target we must find.

It is not just based on the tools we use. They will only do much good if we know what to do with the information we get from them. The tools are just tools, and tools alone should never replace our knowledge and our attention to detail. Here it is much more about actively interacting with the individual services to see what information they provide us and what possibilities they offer us.

It is essential to understand how these services work and what syntax they use for effective communication and interaction with the different services.

This phase aims to improve our knowledge and understanding of the technologies, protocols, and how they work and learn to deal with new information and adapt to our already acquired knowledge. Enumeration is collecting as much information as possible. The more information we have, the easier it will be for us to find vectors of attack.

Imagine the following situation:

Our partner is not at home and has misplaced our car keys. We call our partner and ask where the keys are. If we get an answer like "in the living room," it is entirely unclear and can take much time to find them there. However, what if our partner tells us something like "in the living room on the white shelf, next to the TV, in the third drawer"? As a result, it will be much easier to find them.

It's not hard to get access to the target system once we know how to do it. Most of the ways we can get access we can narrow down to the following two points:

- `Functions and/or resources that allow us to interact with the target and/or provide additional information.`
- `Information that provides us with even more important information to access our target.`

When scanning and inspecting, we look exactly for these two possibilities. Most of the information we get comes from misconfigurations or neglect of security for the respective services. Misconfigurations are either the result of ignorance or a wrong security mindset. For example, if the administrator only relies on the firewall, Group Policy Objects (GPOs), and continuous updates, it is often not enough to secure the network.

`Enumeration is the key`.

That's what most people say, and they are right. However, it is too often misunderstood. Most people understand that they haven't tried all the tools to get the information they need. Most of the time, however, it's not the tools we haven't tried, but rather the fact that we don't know how to interact with the service and what's relevant.

That's precisely the reason why so many people stay stuck in one spot and don't get ahead. Had these people invested a couple of hours learning more about the service, how it works, and what it is meant for, they would save a few hours or even days from reaching their goal and get access to the system.

`Manual enumeration` is a `critical` component. Many scanning tools simplify and accelerate the process. However, these cannot always bypass the security measures of the services. The easiest way to illustrate this is to use the following example:

Most scanning tools use a timeout while waiting to receive a response from a service/port. If this service/port does not respond within a specific amount of time, the tool will mark it as closed, filtered, or unknown. In the last two cases, we will still be able to work with it. However, if a port is marked as closed and Nmap doesn't show it to us, we will be in a bad situation. This service/port may provide us with an opportunity to find a way to access the system. Therefore, this can cost us a lot of unnecessary time before we find it.# Enumeration

---

`Enumeration` is the most critical part of all. The art, the difficulty, and the goal are not to gain access to our target computer. Instead, it is identifying all of the ways we could attack a target we must find.

It is not just based on the tools we use. They will only do much good if we know what to do with the information we get from them. The tools are just tools, and tools alone should never replace our knowledge and our attention to detail. Here it is much more about actively interacting with the individual services to see what information they provide us and what possibilities they offer us.

It is essential to understand how these services work and what syntax they use for effective communication and interaction with the different services.

This phase aims to improve our knowledge and understanding of the technologies, protocols, and how they work and learn to deal with new information and adapt to our already acquired knowledge. Enumeration is collecting as much information as possible. The more information we have, the easier it will be for us to find vectors of attack.

Imagine the following situation:

Our partner is not at home and has misplaced our car keys. We call our partner and ask where the keys are. If we get an answer like "in the living room," it is entirely unclear and can take much time to find them there. However, what if our partner tells us something like "in the living room on the white shelf, next to the TV, in the third drawer"? As a result, it will be much easier to find them.

It's not hard to get access to the target system once we know how to do it. Most of the ways we can get access we can narrow down to the following two points:

- `Functions and/or resources that allow us to interact with the target and/or provide additional information.`
- `Information that provides us with even more important information to access our target.`

When scanning and inspecting, we look exactly for these two possibilities. Most of the information we get comes from misconfigurations or neglect of security for the respective services. Misconfigurations are either the result of ignorance or a wrong security mindset. For example, if the administrator only relies on the firewall, Group Policy Objects (GPOs), and continuous updates, it is often not enough to secure the network.

`Enumeration is the key`.

That's what most people say, and they are right. However, it is too often misunderstood. Most people understand that they haven't tried all the tools to get the information they need. Most of the time, however, it's not the tools we haven't tried, but rather the fact that we don't know how to interact with the service and what's relevant.

That's precisely the reason why so many people stay stuck in one spot and don't get ahead. Had these people invested a couple of hours learning more about the service, how it works, and what it is meant for, they would save a few hours or even days from reaching their goal and get access to the system.

`Manual enumeration` is a `critical` component. Many scanning tools simplify and accelerate the process. However, these cannot always bypass the security measures of the services. The easiest way to illustrate this is to use the following example:

Most scanning tools use a timeout while waiting to receive a response from a service/port. If this service/port does not respond within a specific amount of time, the tool will mark it as closed, filtered, or unknown. In the last two cases, we will still be able to work with it. However, if a port is marked as closed and Nmap doesn't show it to us, we will be in a bad situation. This service/port may provide us with an opportunity to find a way to access the system. Therefore, this can cost us a lot of unnecessary time before we find it.

# What is Enumeration?

**Enumeration is the process of collecting as much information as possible about a target.**

The module says:

> The goal is **not** to gain access immediately. The goal is to identify **every possible way** you _could_ attack the target.

Think of it like being a detective.

Instead of asking:

> "How do I hack this server?"

You ask:

> "What do I know about this server?"

Every new piece of information gives you another clue.

---

# Why is Enumeration So Important?

HTB repeatedly emphasizes:

> **Enumeration is the key.**

Imagine two people attacking the same machine.

### Person A

Immediately starts running exploits.

```
Target

↓

Exploit

↓

Fails
```

They don't know what services exist or what versions are running.

---

### Person B

First enumerates.

```
Target
   │
   ▼
Find open ports
   │
   ▼
Identify services
   │
   ▼
Determine versions
   │
   ▼
Check configurations
   │
   ▼
Choose the correct attack
```

This person has a much higher chance of success.

# The Two Things You're Looking For

According to the module, almost every attack starts from one of these:

### 1. Functions or resources

These are services that let you interact with the target.

Examples:

- SSH
- HTTP
- FTP
- SMB

---

### 2. Information

Sometimes the service itself leaks useful information.

For example:

- Software version
- Usernames
- Hostname
- Shares
- Configuration details

That information can later be used to gain access.

# Tools Are Not Enough

This is one of the most important lessons in the module.

Many beginners think:

> "If I use enough tools, I'll find the vulnerability."

HTB says that's the wrong mindset.

The problem usually isn't that you picked the wrong tool.

The problem is that you don't understand **how the service works** or **how to communicate with it**.

For example:

```
Port 21
```

If you don't know FTP,

Nmap can tell you:

```
FTP
```

…but you still won't know what to do next.

Learning the protocol is often more valuable than running another scanner.

---

# Why Manual Enumeration Matters

Automated scanners are helpful,

but they aren't perfect.

HTB explains that many scanners (including Nmap) use **timeouts**.

Example:

```
Target

↓

Nmap sends request

↓

No response before timeout

↓

Marks port closed
```

But imagine the server was just slow.

The port might actually be open.

If you trust the scan blindly,

you may completely miss an attack path.

---

# Real Penetration Testing Workflow

This is the mindset you'll use throughout HTB, CPTS, and OSCP.

```
Target
   │
   ▼
Discover Hosts
   │
   ▼
Find Open Ports
   │
   ▼
Identify Services
   │
   ▼
Determine Versions
   │
   ▼
Interact Manually
   │
   ▼
Find Misconfigurations
   │
   ▼
Exploit
```

Notice that **exploitation comes after enumeration**, not before.

---

# Key Takeaways

- **Enumeration** = Gather as much information as possible before attacking.
- The goal is to identify **every possible attack surface**, not immediately gain access.
- You're mainly looking for:
    - Services/resources you can interact with.
    - Information those services reveal.
- **Understanding protocols and services** is more valuable than simply running more tools.
- **Manual enumeration** complements automated tools because scanners can miss information due to timeouts or other limitations.

## What to remember for HTB, CPTS, and OSCP

Before you exploit anything, always ask:

1. **What ports are open?**
2. **What services are running?**
3. **What versions are they?**
4. **What information do they reveal?**
5. **How can I interact with each service manually?**

This disciplined approach is the foundation of successful penetration testing.

# Introduction to Nmap

---

Network Mapper (`Nmap`) is an open-source network analysis and security auditing tool written in C, C++, Python, and Lua. It is designed to scan networks and identify which hosts are available on the network using raw packets, and services and applications, including the name and version, where possible. It can also identify the operating systems and versions of these hosts. Besides other features, Nmap also offers scanning capabilities that can determine if packet filters, firewalls, or intrusion detection systems (IDS) are configured as needed.

---

## Use Cases

The tool is one of the most used tools by network administrators and IT security specialists. It is used to:

- Audit the security aspects of networks
- Simulate penetration tests
- Check firewall and IDS settings and configurations
- Types of possible connections
- Network mapping
- Response analysis
- Identify open ports
- Vulnerability assessment as well.

---

## Nmap Architecture

Nmap offers many different types of scans that can be used to obtain various results about our targets. Basically, Nmap can be divided into the following scanning techniques:

- Host discovery
- Port scanning
- Service enumeration and detection
- OS detection
- Scriptable interaction with the target service (Nmap Scripting Engine)

---

## Syntax

The syntax for Nmap is fairly simple and looks like this:

        shellsession
`Akshit023@htb[/htb]$ nmap <scan types> <options> <target>`

---

## Scan Techniques

Nmap offers many different scanning techniques, making different types of connections and using differently structured packets to send. Here we can see all the scanning techniques Nmap offers:

        shellsession
`Akshit023@htb[/htb]$ nmap --help <SNIP> SCAN TECHNIQUES:   -sS/sT/sA/sW/sM: TCP SYN/Connect()/ACK/Window/Maimon scans  -sU: UDP Scan  -sN/sF/sX: TCP Null, FIN, and Xmas scans  --scanflags <flags>: Customize TCP scan flags  -sI <zombie host[:probeport]>: Idle scan  -sY/sZ: SCTP INIT/COOKIE-ECHO scans  -sO: IP protocol scan  -b <FTP relay host>: FTP bounce scan <SNIP>`

For example, the TCP-SYN scan (`-sS`) is one of the default settings unless we have defined otherwise and is also one of the most popular scan methods. This scan method makes it possible to scan several thousand ports per second. The TCP-SYN scan sends one packet with the SYN flag and, therefore, never completes the three-way handshake, which results in not establishing a full TCP connection to the scanned port.

- If our target sends a `SYN-ACK` flagged packet back to us, Nmap detects that the port is `open`.
- If the target responds with an `RST` flagged packet, it is an indicator that the port is `closed`.
- If Nmap does not receive a packet back, it will display it as `filtered`. Depending on the firewall configuration, certain packets may be dropped or ignored by the firewall.

Let us take an example of such a scan.

        shellsession
`Akshit023@htb[/htb]$ sudo nmap -sS localhost Starting Nmap 7.80 ( https://nmap.org ) at 2020-06-11 22:50 UTC Nmap scan report for localhost (127.0.0.1) Host is up (0.000010s latency). Not shown: 996 closed ports PORT     STATE SERVICE 22/tcp   open  ssh 80/tcp   open  http 5432/tcp open  postgresql 5901/tcp open  vnc-1 Nmap done: 1 IP address (1 host up) scanned in 0.18 seconds`

In this example, we can see that we have four different TCP ports open. In the first column, we see the number of the port. Then, in the second column, we see the service's status and then what kind of service it is.

This section introduces **Nmap (Network Mapper)**, the most widely used network enumeration tool in penetration testing. It explains **what Nmap does**, **how it's organized**, and introduces your first scan type: the **TCP SYN scan (`-sS`)**.

---

# What is Nmap?

**Nmap (Network Mapper)** is an open-source tool used to:

- Discover hosts on a network
- Find open ports
- Identify running services
- Detect service versions
- Detect operating systems
- Check firewalls and packet filters

Its main purpose is **network enumeration**—learning as much as possible about a target before attempting exploitation.

---

# Why Do We Use Nmap?

According to the module, Nmap is commonly used for:

- Auditing network security
- Simulating penetration tests
- Checking firewall and IDS configurations
- Network mapping
- Response analysis
- Finding open ports
- Vulnerability assessment

Think of Nmap as your **first reconnaissance tool**.

```
Target
   │
   ▼
Nmap Scan
   │
   ▼
Open Ports
Running Services
Versions
Operating System
```

---

# Nmap Architecture

The module divides Nmap into five major capabilities:

1. **Host Discovery**
2. **Port Scanning**
3. **Service Enumeration & Detection**
4. **OS Detection**
5. **Nmap Scripting Engine (NSE)**

Let's understand each one.

### 1. Host Discovery

Question:

> "Is the machine alive?"

Example:

```
nmap 10.10.10.5
```

---

### 2. Port Scanning

Question:

> "Which ports are open?"

Example:

```
22
80
443
3306
```

---

### 3. Service Detection

Question:

> "What service is running on that port?"

Example:

```
22 → SSH

80 → HTTP

3306 → MySQL
```

---

### 4. OS Detection

Question:

> "What operating system is running?"

Example:

```
Windows

Ubuntu

Debian

FreeBSD
```

---

### 5. NSE (Nmap Scripting Engine)

Nmap can execute scripts against services.

Examples include:

- SMB enumeration
- FTP enumeration
- Vulnerability checks

The module only introduces NSE here; you'll use it later.

---

# Basic Nmap Syntax

Every Nmap command follows the same pattern:

```
nmap <scan types> <options> <target>
```

Example:

```
nmap -sS 10.10.10.5
```

Breakdown:

```
nmap
 │
 ├── Scan Type (-sS)
 ├── Options
 └── Target
```

---

# Scan Techniques

The module lists several scan types.

Some common ones are:

|Option|Purpose|
|---|---|
|`-sS`|TCP SYN Scan|
|`-sT`|TCP Connect Scan|
|`-sU`|UDP Scan|
|`-sN`|NULL Scan|
|`-sF`|FIN Scan|
|`-sX`|Xmas Scan|
|`-sA`|ACK Scan|
|`-sO`|IP Protocol Scan|

Don't try to memorize them all today. Focus on **`-sS`**, since it's the first and most commonly used.

---

# TCP SYN Scan (`-sS`)

This is often called a **half-open scan**.

It sends only the **SYN** packet and does **not** complete the TCP three-way handshake. That makes it fast and commonly used.

### Normal TCP Connection

```
Client                 Server

SYN ------------------>

<---------------- SYN-ACK

ACK ------------------>
```

Connection established.

---

### SYN Scan

```
Client                 Server

SYN ------------------>

<---------------- SYN-ACK

RST ------------------>
```

Instead of completing the handshake, Nmap sends an **RST** (reset), so a full connection is never established.

---

# How Nmap Interprets Responses

### Open Port

Target replies:

```
SYN-ACK
```

Nmap concludes:

```
Open
```

---

### Closed Port

Target replies:

```
RST
```

Nmap concludes:

```
Closed
```

---

### Filtered Port

Target sends nothing (or a firewall silently drops the packet).

Nmap concludes:

```
Filtered
```

The module notes that firewalls may drop or ignore packets, leading to a **filtered** result.

---

# Example Scan

```
sudo nmap -sS localhost
```

Example output:

```
PORT     STATE SERVICE

22/tcp   open  ssh

80/tcp   open  http

5432/tcp open  postgresql

5901/tcp open  vnc-1
```

Let's interpret each column.

### PORT

```
22/tcp
```

Port number and protocol.

---

### STATE

```
open
```

Whether the port is open, closed, or filtered.

---

### SERVICE

```
ssh
```

Nmap's best guess of the service listening on that port.

---

# Real Pentesting Workflow

When you encounter a target:

```
Target
   │
   ▼
Host Discovery
   │
   ▼
Port Scan
   │
   ▼
Service Detection
   │
   ▼
Version Enumeration
   │
   ▼
Manual Enumeration
   │
   ▼
Exploitation
```

Nmap is usually your **first tool** in this process.

---

# Commands to Remember

|Command|Purpose|
|---|---|
|`nmap <target>`|Basic scan|
|`nmap -sS <target>`|TCP SYN scan|
|`sudo nmap -sS localhost`|SYN scan of localhost|
|`nmap --help`|Show scan types and options|

## What to remember for HTB, CPTS, and OSCP

- **Nmap** is your primary network enumeration tool.
- Use the basic syntax: `nmap <scan types> <options> <target>`.
- The **TCP SYN scan (`-sS`)** is one of the fastest and most common scan types because it doesn't complete the full TCP handshake.
- Interpret SYN scan results as:
    - **SYN-ACK → Open**
    - **RST → Closed**
    - **No response → Filtered**
- Always read the three main output columns:
    - **PORT** (port number/protocol)
    - **STATE** (open, closed, filtered)
    - **SERVICE** (detected service)

